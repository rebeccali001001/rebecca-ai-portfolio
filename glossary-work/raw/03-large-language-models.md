# Topic

Large Language Models

Module/Topic/Source File

- Module: 03 · LLMs & Transformers
- Topic: Large Language Models
- Source File: `llm.html`

## Glossary Candidates

| English Term | 中文 | Simple English | 中文小白理解 |
|---|---|---|---|
| Large Language Model | 大型语言模型 | A model trained to work with language tokens. | 学习大量语言 token、能够处理和生成文字的模型。 |
| LLM | 大型语言模型（LLM） | Short name for Large Language Model. | Large Language Model 的英文缩写。 |
| large language model | 大型语言模型 | A language model with many learned parameters and broad language capability. | 参数很多、能处理多种语言任务的语言模型。 |
| language model | 语言模型 | A model that estimates likely sequences of language. | 根据上下文判断哪些词或 token 更可能出现的模型。 |
| model | 模型 | A learned computational system that maps input to output. | 从数据中学会规律、接收输入并产生输出的计算系统。 |
| model component | 模型组件 | One model used as part of a larger product or system. | AI 产品中负责某一部分能力的模型。 |
| generative model | 生成模型 | A model that creates new content from learned patterns. | 根据学到的规律生成新内容的模型。 |
| generative AI | 生成式人工智能 | AI that produces text, images, code, audio, or other content. | 能生成文字、图片、代码等内容的 AI。 |
| Artificial Intelligence | 人工智能 | The broader field of systems that perform tasks associated with intelligence. | 比 LLM 更大的领域，包含各种智能系统和方法。 |
| AI | 人工智能（AI） | Short name for Artificial Intelligence. | Artificial Intelligence 的英文缩写。 |
| NLP | 自然语言处理（NLP） | The field of working with human language using computers. | 让计算机理解、处理和生成自然语言的领域。 |
| natural language | 自然语言 | Language used by people, such as English or Chinese. | 人类日常使用的语言。 |
| text | 文本 | Written language represented as characters or tokens. | 由字符或 token 表示的文字内容。 |
| language | 语言 | A structured system used to communicate meaning. | 人们用来表达意思和交流的系统。 |
| token | token；词元 | A small unit of text processed by a language model. | 模型实际处理的文字小片段，不一定等于一个完整单词。 |
| language token | 语言 token | A token representing part of a language sequence. | 表示语言序列一部分的模型输入单位。 |
| tokenization | 分词；标记化 | Splitting text into tokens. | 把句子切成模型可以处理的小片段。 |
| tokenizer | 分词器；标记器 | The component that converts text into tokens and back. | 负责把文字转成 token、再把 token 还原成文字的组件。 |
| subword token | 子词 token | A token representing a whole word or part of a word. | 可以是完整单词，也可以只是单词的一部分。 |
| character | 字符 | A single written symbol in text. | 文本中的单个字母、汉字或符号。 |
| word | 单词 | A meaningful unit of language, sometimes split into tokens. | 有意义的语言单位，但模型可能把它拆成多个 token。 |
| vocabulary | 词表 | The set of tokens a model knows how to represent. | 模型可以识别和输出的 token 集合。 |
| sequence | 序列 | An ordered list of tokens. | 按顺序排列的一串 token。 |
| token sequence | token 序列 | Text represented as an ordered list of tokens. | 把文字转换成 token 后形成的有序列表。 |
| context | 上下文 | Earlier information used to interpret or continue text. | 模型生成下一步时参考的前文信息。 |
| contextual information | 上下文信息 | Information available from the current sequence. | 当前输入序列中可供模型参考的信息。 |
| prompt | 提示词；提示 | Text or instructions supplied to a model. | 用户或系统给模型的输入和要求。 |
| user question | 用户问题 | A question sent by a user to an AI product. | 用户输入给 AI 产品的问题。 |
| instruction | 指令 | A request describing what the model should do. | 告诉模型要完成什么任务的要求。 |
| input | 输入 | Information sent into a model or system. | 送进模型或系统的信息。 |
| input tokens | 输入 token | Tokens received from the prompt or context. | prompt 或上下文被切分后的输入片段。 |
| output | 输出 | The result returned by a model or system. | 模型或系统返回的结果。 |
| output tokens | 输出 token | Tokens generated by the model. | 模型生成结果中的 token。 |
| next token | 下一个 token | The token selected to extend the current sequence. | 接在当前序列后面的下一个文字片段。 |
| next-token prediction | 下一个 token 预测 | Estimating which token should come next. | 根据前文预测下一小段文字。 |
| next-token scores | 下一个 token 分数 | Scores assigned to possible next tokens. | 模型给每个候选下一个 token 的相对分数。 |
| candidate token | 候选 token | One possible token that could come next. | 模型认为可能接在后面的某个 token。 |
| continuation | 延续内容 | Text that continues an existing sequence. | 接着已有文字继续写出的内容。 |
| autocomplete | 自动补全 | Suggesting a likely continuation of partial text. | 根据已经输入的开头自动补出后面的内容。 |
| autocomplete engine | 自动补全引擎 | A system that predicts likely text continuations. | 像高级自动补全一样预测后续文字的系统。 |
| statistical pattern | 统计模式 | A regularity learned from frequencies and relationships in data. | 从大量数据的频率和关系中学到的规律。 |
| pattern | 模式；规律 | A repeated relationship found in examples. | 例子中反复出现、可以被利用的关系。 |
| learned pattern | 学到的模式 | A relationship inferred from training data. | 模型从训练数据中归纳出来的规律。 |
| text dataset | 文本数据集 | A collection of text used for training or evaluation. | 用来训练或评估模型的一批文字数据。 |
| dataset | 数据集 | An organized collection of examples or records. | 按一定方式整理的一组数据。 |
| corpus | 语料库 | A large collection of language data. | 用于语言研究或模型训练的大型文本集合。 |
| training data | 训练数据 | Data used to teach a model patterns. | 模型学习语言规律时使用的数据。 |
| language data | 语言数据 | Data containing human language. | 包含文字、句子、对话或文档的数据。 |
| predict | 预测 | Estimate a likely next item or outcome. | 根据已有信息猜测下一步或结果。 |
| prediction | 预测 | An estimate of what is likely to happen next. | 模型对下一 token 或结果的估计。 |
| probability | 概率 | A value representing how likely an option is. | 表示某个候选出现可能性大小的数值。 |
| probability distribution | 概率分布 | Probabilities assigned across multiple possible options. | 把可能的下一个 token 及其概率排列起来。 |
| likelihood | 可能性 | How plausible an option is under the model. | 模型认为某个选项有多可能。 |
| score | 分数 | A numerical value used to compare candidate options. | 用来比较候选 token 的数值。 |
| logit | logit；未归一化分数 | A raw model score before conversion to probabilities. | 转成概率前模型输出的原始分数。 |
| sampling | 采样 | Selecting an output from a probability distribution. | 按候选概率选择下一个 token 的过程。 |
| greedy decoding | 贪心解码 | Always selecting the highest-scoring next token. | 每一步都选分数最高的 token。 |
| decoding | 解码 | Turning model token choices into an output sequence. | 把模型选择的 token 变成最终文字的过程。 |
| readable text | 可读文本 | Text reconstructed from generated tokens. | 把 token 还原后人能够阅读的文字。 |
| generated text | 生成文本 | Text produced by a generative model. | 模型根据输入生成的新文字。 |
| generation | 生成 | Producing a new sequence from a prompt and context. | 根据输入和上下文逐步写出新内容。 |
| continuation generation | 延续生成 | Generating text that follows an existing sequence. | 接着已有文本继续生成。 |
| stopping condition | 停止条件 | A rule that tells generation when to end. | 告诉模型什么时候结束生成的条件。 |
| end-of-sequence token | 序列结束 token | A special token indicating that a sequence can end. | 表示一段 token 序列可以结束的特殊 token。 |
| maximum token limit | 最大 token 限制 | The maximum number of tokens accepted or generated. | 一次输入或输出允许的 token 数量上限。 |
| generation loop | 生成循环 | The repeated process of predicting and appending tokens. | 反复预测并追加 token 的循环。 |
| next-token loop | 下一个 token 循环 | Repeatedly predicting one next token at a time. | 一次预测一个 token、不断重复的过程。 |
| autoregressive generation | 自回归生成 | Generating each token using tokens already in the sequence. | 每生成一个 token，就把它作为后续预测的新上下文。 |
| autoregressive model | 自回归模型 | A model that predicts later values from earlier values. | 根据前面内容预测后面内容的模型。 |
| causal language model | 因果语言模型 | A language model that predicts the future from past context. | 只能用前文预测后文的语言模型。 |
| causal mask | 因果掩码 | A mask preventing a position from seeing future tokens. | 防止模型偷看后文 token 的机制。 |
| Transformer | Transformer 架构 | A neural architecture commonly used in modern language models. | 现代 LLM 常用的神经网络架构。 |
| Transformer layer | Transformer 层 | One repeated processing block in a Transformer. | Transformer 中重复堆叠、处理上下文的一层。 |
| Transformer block | Transformer 模块 | A complete repeated unit containing attention and feed-forward processing. | 通常包含注意力和前馈处理的一组网络结构。 |
| attention | 注意力机制 | A mechanism for weighting relevant parts of a sequence. | 让模型判断当前 token 应该重点参考哪些位置。 |
| self-attention | 自注意力 | Attention computed among tokens in the same sequence. | 序列中的 token 彼此计算相关性的注意力。 |
| attention weights | 注意力权重 | Values showing how strongly positions influence one another. | 表示不同位置相互参考程度的数值。 |
| query | 查询向量；Query | A representation used to ask what information is relevant. | 当前 token 用来寻找相关信息的向量。 |
| key | 键向量；Key | A representation used to match relevant information. | 被其他 token 用来匹配的向量。 |
| value | 值向量；Value | The information retrieved after an attention match. | 注意力匹配后真正被取用的信息向量。 |
| contextual representation | 上下文表示 | A representation whose meaning reflects surrounding tokens. | 融合前后文后、能体现语境的 token 表示。 |
| embedding | 嵌入向量 | A numeric vector representing a token or piece of text. | 用数字向量表示 token 或文字含义。 |
| token embedding | token 嵌入 | The vector representation initially assigned to a token. | 模型开始处理时给 token 的数字表示。 |
| positional encoding | 位置编码 | Information representing where a token occurs in a sequence. | 告诉模型每个 token 在序列中的位置。 |
| position embedding | 位置嵌入 | A learned or computed vector for sequence position. | 表示序列位置的向量。 |
| hidden state | 隐藏状态 | An internal representation produced while processing input. | 模型内部在处理过程中保存的信息表示。 |
| hidden representation | 隐藏表示 | Internal numeric information about an input. | 模型内部对输入形成的数字化理解。 |
| neural network | 神经网络 | A layered function that learns transformations from data. | 由多层计算组成、能从数据学习规律的模型。 |
| neural network layer | 神经网络层 | One transformation stage in a neural network. | 神经网络中完成一次特征变换的一层。 |
| layer | 层 | A repeated computational stage in a model. | 模型中按顺序处理信息的一步结构。 |
| feed-forward network | 前馈网络 | A network that transforms each position after attention. | 注意力之后进一步变换每个位置表示的网络。 |
| MLP | 多层感知机（MLP） | A multilayer feed-forward neural network. | 多层前馈神经网络的常见缩写。 |
| residual connection | 残差连接 | A shortcut that adds an earlier representation to a later one. | 把前面的表示直接加回后面，帮助信息和梯度传递。 |
| layer normalization | 层归一化 | Normalizing activations within a network layer. | 对层内数值进行规范化以稳定训练。 |
| activation | 激活值 | The numeric output of a neural network operation. | 神经网络某一步计算产生的数值。 |
| output head | 输出头 | The final component that maps hidden states to predictions. | 把隐藏状态转成候选 token 分数的最后一部分。 |
| softmax | Softmax 函数 | A function that converts scores into a probability distribution. | 把分数转换成总和为 1 的概率。 |
| vocabulary projection | 词表投影 | Mapping hidden representations to scores over the vocabulary. | 把内部表示映射成整个词表中各 token 的分数。 |
| context window | 上下文窗口 | The amount of token context a model can consider at once. | 模型一次能看到和参考的 token 范围。 |
| long-context model | 长上下文模型 | A model designed to handle unusually long token sequences. | 能处理很长输入内容的模型。 |
| context length | 上下文长度 | The number of tokens available in a model context. | 当前上下文最多能容纳的 token 数量。 |
| current sequence | 当前序列 | The tokens available at the current generation step. | 模型此刻已经收到或生成的全部 token。 |
| prompt context | 提示上下文 | The instructions and information supplied with a prompt. | prompt 中给模型参考的背景和要求。 |
| process context | 处理上下文 | Combining information from the current sequence. | 模型综合当前序列信息的过程。 |
| receive text | 接收文本 | Accepting text as model input. | 模型收到文字输入。 |
| split into tokens | 切分为 token | Converting text into token units. | 把文字切成模型能处理的小片段。 |
| combine information | 组合信息 | Integrating information from multiple positions. | 把序列不同位置的信息融合起来。 |
| estimate options | 估计选项 | Score possible next tokens. | 为多个可能的下一 token 计算分数或概率。 |
| selected token | 被选中的 token | The candidate chosen at one generation step. | 当前一步最终选出来的 token。 |
| append | 追加 | Add a selected token to the end of a sequence. | 把新 token 接到已有序列末尾。 |
| extend the answer | 延长答案 | Continue generating the response. | 继续生成更多回答内容。 |
| token sequence decoding | token 序列解码 | Converting an ordered token sequence into text. | 将 token 序列还原为人类可读的文字。 |
| input-output flow | 输入输出流程 | The path from received input to returned output. | 从接收输入到返回结果的完整过程。 |
| system | 系统 | The complete mechanism around a model. | 包含模型及其输入、输出和其他组件的整体。 |
| AI product | AI 产品 | A user-facing product that may include a model and other systems. | 面向用户的产品，通常不只有一个 LLM。 |
| ChatGPT | ChatGPT | A product built with models, policies, interface, and operations. | 使用模型、政策、界面和运营系统组成的产品。 |
| product | 产品 | A complete user-facing service built from components. | 用户实际使用的完整服务。 |
| interface | 界面 | The part through which users interact with a product. | 用户输入问题、查看结果的交互界面。 |
| policy | 政策；规则 | Rules governing how a product or model should behave. | 规定产品或模型应如何工作的规则。 |
| operations | 运营与运维 | The processes used to run and maintain a product. | 让产品持续运行、监控和维护的工作。 |
| product system | 产品系统 | The model plus surrounding product services and controls. | 模型外加产品服务与控制机制组成的整体。 |
| model-plus-system | 模型加系统 | A model working together with retrieval, tools, memory, or controls. | 模型与检索、工具、记忆、安全控制等一起工作的组合。 |
| retrieval | 检索 | Finding existing information from a source. | 从文档、数据库或其他来源找已有信息。 |
| retrieval system | 检索系统 | A system that fetches relevant existing information. | 负责找出相关资料的系统。 |
| RAG | 检索增强生成（RAG） | Generation that uses retrieved external information. | 先检索资料、再让模型结合资料生成回答的方法。 |
| grounding | 基于来源落地；接地 | Tying a response to known external information. | 让回答有资料依据，而不是只靠模型记忆。 |
| citation | 引用；来源标注 | A reference showing where supporting information came from. | 标出回答依据来源的引用。 |
| tool | 工具 | An external capability an AI system can call. | 模型可以调用的搜索、计算或业务功能。 |
| tool use | 工具使用 | Calling external tools to complete a task. | 让 AI 调用外部功能完成任务。 |
| function calling | 函数调用 | Structured requests for an external function or API. | 模型按结构化格式请求外部函数或 API。 |
| memory | 记忆 | Information stored for use across turns or tasks. | 系统保存下来、以后继续参考的信息。 |
| safety control | 安全控制 | A mechanism that limits harmful or unwanted behavior. | 限制危险、不当或不希望行为的机制。 |
| guardrail | 防护栏；安全护栏 | A rule or control that constrains model behavior. | 限制模型行为边界的规则或技术措施。 |
| content filter | 内容过滤器 | A component that detects or blocks certain content. | 检测并拦截特定内容的组件。 |
| moderation | 内容审核 | Reviewing or classifying content for policy compliance. | 判断内容是否符合安全和平台规则。 |
| system prompt | 系统提示词 | High-priority instructions that define model behavior. | 在用户问题前由系统设置的高优先级要求。 |
| developer instruction | 开发者指令 | Instructions supplied by an application developer. | 应用开发者给模型设定的行为要求。 |
| prompt injection | 提示词注入 | An attempt to make a model ignore or override intended instructions. | 通过输入诱导模型绕过原本指令的攻击方式。 |
| jailbreak | 越狱 | An attempt to bypass model safety restrictions. | 试图绕过模型安全限制的做法。 |
| data leakage | 数据泄露 | Unintended exposure of private or restricted information. | 不该公开的信息被模型或系统暴露出来。 |
| privacy | 隐私 | Protection of personal or confidential information. | 对个人信息和机密信息的保护。 |
| PII | 个人可识别信息（PII） | Information that can identify a person. | 能识别某个具体个人的信息。 |
| hallucination | 幻觉 | A confident-looking but unsupported or incorrect model output. | 模型说得像真的一样，但内容没有依据或是错误的情况。 |
| factuality | 事实准确性 | Whether an output matches verifiable facts. | 回答是否与可验证事实一致。 |
| reliability | 可靠性 | How consistently a system produces useful and correct results. | 系统能否稳定地产生有用、正确的结果。 |
| human review | 人工复核 | A person checking generated output before use. | 人在使用前检查模型生成内容。 |
| review | 复核；审阅 | Inspecting an output for quality or correctness. | 对结果进行检查和判断。 |
| chat assistant | 聊天助手 | A product that uses a model to respond conversationally. | 用模型与用户进行对话的产品功能。 |
| suggested completion | 建议补全 | Text proposed as a continuation of an input. | 系统建议接在已有输入后面的文字。 |
| meeting transcript | 会议转录稿 | Text representing spoken content from a meeting. | 会议语音被转成的文字记录。 |
| summary | 摘要；总结 | A shorter representation of longer content. | 用更短的文字概括长内容。 |
| draft summary | 摘要草稿 | A generated summary that still needs review. | 由模型生成、还需要人检查的总结初稿。 |
| generated reply | 生成式回复 | A response produced by the model or product. | 模型或产品自动写出的回答。 |
| language task | 语言任务 | A task involving understanding or generating language. | 需要理解或生成文字的任务。 |
| text generation | 文本生成 | Producing text from an input and learned patterns. | 根据输入和学到的语言规律生成文字。 |
| question answering | 问答 | Producing an answer to a user question. | 针对用户问题生成回答。 |
| summarization | 摘要生成 | Condensing a longer text into a shorter one. | 把长文本压缩成较短摘要。 |
| translation | 翻译 | Converting text from one language to another. | 把一种语言的文字转换成另一种语言。 |
| code generation | 代码生成 | Producing programming code from an instruction. | 根据要求自动写出程序代码。 |
| instruction following | 指令遵循 | Producing output that follows a stated request. | 按用户或系统要求完成任务。 |
| conversational model | 对话模型 | A model adapted to respond in a dialogue. | 专门适配多轮对话的模型。 |
| pre-trained model | 预训练模型 | A model trained broadly before task-specific adaptation. | 先用大量通用数据训练好的模型。 |
| base model | 基础模型 | A pretrained model before instruction or task tuning. | 还没有针对具体对话或任务调优的基础版本。 |
| instruction-tuned model | 指令微调模型 | A model tuned to follow human-written instructions. | 经过训练、更擅长按指令办事的模型。 |
| chat model | 聊天模型 | A model tuned for conversational interaction. | 针对聊天形式进行优化的语言模型。 |
| foundation model | 基础模型；基座模型 | A broadly pretrained model used for many downstream tasks. | 先广泛训练、可作为多种应用基础的模型。 |
| frontier model | 前沿模型 | A model near the leading edge of capability. | 能力处于当前前沿的大型模型。 |
| model family | 模型家族 | Related models sharing an architecture or training lineage. | 具有共同架构或训练来源的一组模型。 |
| model type | 模型类型 | A category describing how a model is built or used. | 按结构、训练方式或用途划分的模型类别。 |
| encoder-only model | 仅编码器模型 | A model focused on representing or understanding input. | 主要把输入编码成表示、用于理解任务的模型。 |
| decoder-only model | 仅解码器模型 | A model that generates tokens from preceding context. | 根据前文逐步生成 token 的模型，现代 GPT 类模型常用。 |
| encoder-decoder model | 编码器-解码器模型 | A model that encodes input and then generates output. | 先理解输入、再生成输出的模型结构。 |
| multimodal model | 多模态模型 | A model that handles more than one data modality. | 能处理文字、图片、音频等多种信息的模型。 |
| small language model | 小型语言模型 | A language model with fewer parameters or lower resource needs. | 规模较小、运行资源需求较低的语言模型。 |
| open-weight model | 开放权重模型 | A model whose learned weights are made available. | 模型训练后的权重可以被用户获取或使用。 |
| closed model | 闭源模型 | A model whose weights or implementation are not openly available. | 权重或实现不公开、通常通过服务使用的模型。 |
| local model | 本地模型 | A model run on a user or organization's own hardware. | 在自己的电脑或服务器上运行的模型。 |
| cloud model | 云端模型 | A model hosted and served by a remote provider. | 由远程云服务商托管并提供调用的模型。 |
| dense model | 稠密模型 | A model that activates most parameters for each input. | 每次处理输入时大多数参数都会参与计算的模型。 |
| mixture of experts | 混合专家模型 | A model that routes each input to selected expert networks. | 根据输入只调用部分专家网络的模型结构。 |
| MoE | 混合专家（MoE） | Short name for Mixture of Experts. | Mixture of Experts 的英文缩写。 |
| expert network | 专家网络 | A specialized subnetwork inside a mixture-of-experts model. | 混合专家模型内部负责特定模式的子网络。 |
| router | 路由器；路由模块 | A component that selects experts or processing paths. | 决定输入交给哪些专家或路径处理的组件。 |
| model parameter | 模型参数 | A learned numeric value inside a model. | 模型训练中学到的数字。 |
| parameter | 参数 | A learned value that controls a model's behavior. | 决定模型如何计算和输出的可学习数值。 |
| weights | 权重 | Learned values that determine the strength of connections. | 神经网络连接强弱的数字表示。 |
| model size | 模型规模 | A measure often based on parameter count or resource needs. | 通常用参数数量或运行资源衡量的模型大小。 |
| parameter count | 参数量 | The number of learned values in a model. | 模型中可学习数字的数量。 |
| architecture | 架构 | The arrangement of components and layers in a model. | 模型由哪些组件组成、如何连接的结构设计。 |
| model family name | 模型家族名称 | A name shared by related model versions. | 一组相关模型共同使用的系列名称。 |
| GPT | 生成式预训练 Transformer（GPT） | A family name for Generative Pre-trained Transformer models. | Generative Pre-trained Transformer 的缩写，也是常见模型家族名称。 |
| generative pre-trained transformer | 生成式预训练 Transformer | A Transformer pretrained to generate sequences. | 先进行通用预训练、再用 Transformer 生成序列的模型类型。 |
| training | 训练 | Adjusting model parameters using data. | 用数据不断调整模型参数，让模型学会规律。 |
| pretraining | 预训练 | Broad initial training on large general datasets. | 先用大量通用数据进行基础学习。 |
| fine-tuning | 微调 | Further training a pretrained model for a target task or behavior. | 在已有模型基础上继续训练，使其适应特定任务。 |
| supervised fine-tuning | 监督微调 | Fine-tuning with examples containing desired outputs. | 用“输入-正确答案”示例继续训练模型。 |
| SFT | 监督微调（SFT） | Short name for Supervised Fine-Tuning. | Supervised Fine-Tuning 的英文缩写。 |
| instruction tuning | 指令调优 | Training a model to respond appropriately to instructions. | 训练模型更好地理解并执行指令。 |
| preference optimization | 偏好优化 | Training toward outputs preferred by people or a preference model. | 让模型更倾向于产生人们偏好的结果。 |
| RLHF | 基于人类反馈的强化学习（RLHF） | Training with human preference feedback. | 用人类对回答的偏好反馈改进模型。 |
| reinforcement learning from human feedback | 基于人类反馈的强化学习 | A post-training approach using human feedback signals. | 通过人类反馈信号进一步训练模型的方法。 |
| training objective | 训练目标 | The quantity a training process tries to optimize. | 训练时希望模型做到更好的目标。 |
| objective function | 目标函数 | A mathematical function describing the training goal. | 用数学方式表示训练目标的函数。 |
| loss | 损失 | A number measuring how far a prediction is from the target. | 衡量模型预测和目标差距的数值。 |
| loss function | 损失函数 | A function that calculates training error. | 计算模型错误程度、指导训练的函数。 |
| cross-entropy loss | 交叉熵损失 | A common loss for predicting the correct token among choices. | 语言模型预测 token 时常用的错误度量。 |
| gradient | 梯度 | A direction indicating how parameters should change. | 指示参数应该往哪个方向调整的数学信息。 |
| gradient descent | 梯度下降 | Updating parameters to reduce loss. | 逐步调整参数，让损失变小的优化方法。 |
| backpropagation | 反向传播 | Computing how each parameter contributed to error. | 把错误从输出层传回网络、计算参数如何调整。 |
| optimizer | 优化器 | An algorithm that updates parameters during training. | 按梯度更新模型参数的训练算法。 |
| learning rate | 学习率 | The step size used when updating parameters. | 每次调整参数幅度大小的设置。 |
| batch | 批次 | A group of training examples processed together. | 一次同时处理的一组训练样本。 |
| batch size | 批次大小 | The number of examples in one training batch. | 每批训练样本的数量。 |
| epoch | 训练轮次 | One pass through a training dataset. | 把整个训练数据集完整学习一遍。 |
| checkpoint | 检查点 | A saved version of model parameters during training. | 训练过程中保存的一版模型状态。 |
| validation set | 验证集 | Data used to check progress during model development. | 训练过程中用来检查效果的数据。 |
| test set | 测试集 | Held-out data used for final evaluation. | 最后评估模型、训练时不直接使用的一组数据。 |
| generalization | 泛化 | Performing well on new data beyond training examples. | 面对没见过的新数据也能表现良好。 |
| overfitting | 过拟合 | Memorizing training examples instead of learning reusable patterns. | 只记住训练例子，遇到新内容就表现不好。 |
| inference | 推理；推断 | Running a trained model to produce an output. | 使用已经训练好的模型得到结果。 |
| inference-time generation | 推理时生成 | Generating tokens while serving a user request. | 模型处理用户请求时逐步生成 token。 |
| forward pass | 前向传播 | Computing an output by passing input through the network. | 让输入依次通过各层、计算出结果。 |
| training-time | 训练时 | The phase when parameters are learned or updated. | 模型学习和更新参数的阶段。 |
| inference-time | 推理时 | The phase when a trained model serves inputs. | 训练结束后模型实际接受输入并输出的阶段。 |
| model serving | 模型服务 | Making a trained model available to applications. | 把训练好的模型部署成可供应用调用的服务。 |
| serving | 服务化 | Operating a model so it can respond to requests. | 让模型持续接收请求并返回结果。 |
| API | 应用程序接口（API） | A structured interface for software to call a model. | 软件按照规定方式调用模型的接口。 |
| model API | 模型 API | An API endpoint that accepts model requests. | 接收输入、返回模型结果的接口。 |
| request | 请求 | A message asking a model or service to do work. | 发给模型或服务的一次任务要求。 |
| response | 响应 | The result returned for a request. | 服务针对请求返回的结果。 |
| latency | 延迟 | Time between sending a request and receiving a result. | 从发出请求到收到结果所需的时间。 |
| throughput | 吞吐量 | The amount of work served per unit of time. | 单位时间内系统能处理的请求或 token 数量。 |
| token throughput | token 吞吐量 | The number of tokens processed or generated per unit of time. | 单位时间处理或生成的 token 数量。 |
| streaming | 流式输出 | Returning generated output incrementally as it is produced. | 模型边生成边把结果传给用户。 |
| batching | 批处理 | Handling multiple inputs together for efficiency. | 把多个请求合并处理以提高效率。 |
| continuous batching | 连续批处理 | Dynamically adding requests to active batches. | 在运行中动态把新请求加入批次。 |
| KV cache | 键值缓存（KV cache） | Cached attention keys and values reused during generation. | 生成后续 token 时复用前面注意力计算结果的缓存。 |
| cache | 缓存 | Stored intermediate data reused to avoid repeated work. | 保存中间结果、减少重复计算的存储。 |
| GPU | 图形处理器（GPU） | Hardware commonly used for parallel model computation. | 适合大量并行计算、常用来运行 LLM 的硬件。 |
| CPU | 中央处理器（CPU） | General-purpose hardware that can also run model computation. | 通用计算处理器，也可以运行模型。 |
| accelerator | 加速器 | Hardware specialized for faster model computation. | 专门帮助模型更快计算的硬件。 |
| memory footprint | 内存占用 | The amount of memory needed to load and run a model. | 加载和运行模型需要占用的内存大小。 |
| quantization | 量化 | Representing model values with lower numerical precision. | 用更少的数字精度表示模型，降低资源占用。 |
| quantized model | 量化模型 | A model whose weights or activations use reduced precision. | 参数或激活值使用较低精度表示的模型。 |
| distillation | 知识蒸馏 | Training a smaller model to imitate a larger model. | 让小模型学习大模型能力的压缩方法。 |
| compression | 模型压缩 | Reducing model size or compute needs. | 在尽量保持能力的同时减少模型大小或计算量。 |
| deployment | 部署 | Putting a model or application into an environment where users can use it. | 把模型或应用放到真实运行环境中。 |
| production | 生产环境 | The live environment serving real users. | 真正为用户提供服务的运行环境。 |
| local deployment | 本地部署 | Running a model on local or organization-controlled hardware. | 在本地或自己控制的服务器上运行模型。 |
| cloud deployment | 云端部署 | Running a model on provider-managed remote infrastructure. | 在云服务商管理的远程基础设施上运行模型。 |
| model runtime | 模型运行时 | Software that loads and executes a model. | 负责加载模型并执行推理的软件环境。 |
| scalability | 可扩展性 | The ability to handle more users or workload. | 用户和请求增加时系统继续工作的能力。 |
| cost per token | 每 token 成本 | The cost associated with processing or generating tokens. | 处理或生成 token 时产生的费用。 |
| evaluation | 评估 | Measuring model quality against tasks or criteria. | 用任务、数据和指标衡量模型效果。 |
| benchmark | 基准测试 | A standardized task or dataset for comparing models. | 用统一任务或数据比较不同模型的方法。 |
| metric | 指标 | A measurable value used to judge performance. | 用数字衡量模型表现的标准。 |
| accuracy | 准确率 | The proportion of predictions that are correct. | 预测正确的比例。 |
| perplexity | 困惑度 | A language-model metric related to uncertainty in token prediction. | 衡量模型预测语言 token 时不确定程度的指标。 |
| helpfulness | 有用性 | How useful an output is for the user's goal. | 回答对用户目标有多大帮助。 |
| correctness | 正确性 | Whether an output is accurate for the task. | 输出是否正确。 |
| instruction-following score | 指令遵循分数 | A measure of how well a model follows requests. | 衡量模型是否按要求完成任务的分数。 |
| safety evaluation | 安全评估 | Testing whether a model avoids harmful behavior. | 检查模型是否会产生危险行为的评估。 |
| robustness | 稳健性 | Ability to remain reliable under varied inputs or conditions. | 输入变化或受到干扰时仍能稳定工作的能力。 |
| prompt engineering | 提示词工程 | Designing prompts to guide model behavior. | 通过设计输入来更好地引导模型输出。 |
| temperature | 温度参数 | A decoding setting that controls output randomness. | 控制输出随机程度的生成设置。 |
| top-k sampling | Top-k 采样 | Sampling only from the k highest-scoring candidates. | 只在分数最高的 k 个候选中选择。 |
| top-p sampling | Top-p 采样 | Sampling from the smallest candidate set reaching probability p. | 从累计概率达到 p 的最小候选集合中采样。 |
| max tokens | 最大 token 数 | A setting limiting generated output length. | 限制模型最多生成多少 token 的设置。 |
| seed | 随机种子 | A value used to make stochastic generation repeatable. | 让随机生成结果更容易复现的设置。 |
| deterministic decoding | 确定性解码 | Decoding that produces the same output under the same conditions. | 条件相同就尽量生成相同结果的解码方式。 |
| nondeterministic generation | 非确定性生成 | Generation that may vary across runs. | 同样输入多次运行可能得到不同结果。 |
| search engine | 搜索引擎 | A system that retrieves existing information from an index or web. | 从网页或索引中找已有信息的系统。 |
| retrieval versus generation | 检索与生成的区别 | Retrieval finds existing information; generation creates text. | 搜索是找已有资料，LLM 生成是根据规律写出文字。 |
| database | 数据库 | A structured store of existing information. | 保存已有记录、可按条件查询的信息系统。 |
| fact list | 事实清单 | A guaranteed collection of verified facts. | 已经确认、可直接查询的事实集合。 |
| guaranteed fact | 有保证的事实 | Information supported as verified and correct. | 有明确依据、可以验证的正确事实。 |
| learned knowledge | 学到的知识 | Information encoded indirectly in model parameters. | 通过训练规律间接存进模型参数的信息。 |
| parametric memory | 参数化记忆 | Information represented in learned model parameters. | 不是数据库记录，而是分散编码在参数里的信息。 |
| source of truth | 权威事实来源 | A trusted source used to verify information. | 用来核实事实是否正确的可信来源。 |
| model output | 模型输出 | The text or tokens produced by a model. | 模型生成的 token 或最终文字。 |
| model capability | 模型能力 | A task the model can perform with some level of quality. | 模型能够完成的任务范围和效果。 |
| model limitation | 模型局限 | A condition under which a model may fail or be unreliable. | 模型可能出错、不可靠或无法完成任务的情况。 |
| useful continuation | 有用的延续 | A generated continuation that helps complete a request. | 能帮助用户完成目标的后续文字。 |
| unsupported answer | 无依据回答 | An answer not backed by available evidence. | 没有可靠资料支持的回答。 |
| reliable answer | 可靠回答 | An answer that is accurate and appropriately supported. | 内容正确且有适当依据的回答。 |
| component boundary | 组件边界 | The distinction between a model and surrounding product systems. | 区分模型本身与产品其他系统的范围。 |
| model-product distinction | 模型与产品的区别 | The difference between an LLM and a complete AI product. | LLM 是一个模型，ChatGPT 是包含很多系统的产品。 |
| AI field | AI 领域 | The broad area containing many kinds of intelligent systems. | 包含 LLM、视觉、机器人等方向的更大领域。 |
| broader field | 更广泛领域 | A category that contains a narrower technology. | 包含某个具体技术的大范围概念。 |
| related concept | 相关概念 | A concept connected to the current topic. | 与当前主题有直接联系、可继续学习的概念。 |
| terminology alias | 术语别名 | Another name used for the same or closely related concept. | 同一概念或近似概念的另一种叫法。 |

## Potential Missing Concepts

- Tokenizer internals, vocabulary IDs, special tokens, padding tokens, and attention masks.
- Transformer details: multi-head attention, feed-forward network dimensions, residual stream, normalization order, and positional encodings such as RoPE.
- Training mechanics: cross-entropy objective, gradient descent, backpropagation, optimizers, learning-rate schedules, batches, epochs, checkpoints, validation, and test sets.
- Post-training: supervised fine-tuning (SFT), instruction tuning, preference optimization, RLHF, direct preference optimization (DPO), and reward models.
- Inference controls: temperature, top-k, top-p, greedy decoding, beam search, repetition penalty, stop sequences, maximum output tokens, and random seeds.
- Model families and structures: decoder-only, encoder-only, encoder-decoder, dense models, mixture of experts (MoE), multimodal models, embedding models, code models, multilingual models, open-weight models, and closed models.
- Long-context concepts: context window, context length, truncation, sliding-window attention, retrieval-augmented generation (RAG), and lost-in-the-middle behavior.
- Serving and deployment: inference server, model runtime, batching, continuous batching, streaming, KV cache, latency, throughput, autoscaling, GPU memory, quantization, distillation, and cost per token.
- Quality and evaluation: perplexity, accuracy, factuality, helpfulness, instruction following, robustness, toxicity, bias, calibration, benchmarks, human evaluation, and red teaming.
- Safety and security: hallucination, grounding, citations, prompt injection, jailbreaks, system prompts, guardrails, moderation, privacy, PII, data leakage, memorization, copyright, and human review.
- Product integration: APIs, function calling, tool use, retrieval, external databases, memory, conversation state, policies, interfaces, observability, logging, and operations.
- Common model abbreviations and families: GPT, NLP, LLM, RAG, RLHF, SFT, DPO, MoE, API, GPU, CPU, and KV cache.

## Aliases / Synonyms

- Large Language Model / LLM / large language model
- language model / neural language model / generative language model
- token / language token / text token / subword token
- tokenization / text splitting / token splitting
- next-token prediction / next-word prediction / autoregressive prediction
- next-token loop / generation loop / autoregressive generation loop
- generate text / text generation / language generation
- prompt / input text / model instruction / user request
- context / prompt context / input context / sequence context
- Transformer / Transformer architecture / Transformer model
- Transformer layer / Transformer block / decoder block
- attention / self-attention / attention mechanism
- embedding / token embedding / vector representation
- parameters / model parameters / weights / learned weights
- inference / model inference / serving-time generation
- decoding / token decoding / output decoding
- probability / token probability / next-token likelihood
- score / token score / logit score
- stopping condition / stop sequence / end-of-sequence condition
- ChatGPT / chat assistant / AI chat product
- AI product / model-powered product / model-plus-system
- retrieval / information retrieval / external lookup
- retrieval-augmented generation / RAG / grounded generation
- tools / tool use / function calling / external capabilities
- safety controls / safeguards / guardrails / policy controls
- hallucination / unsupported generation / fabricated answer
- training / model training / parameter learning
- pretraining / initial training / broad language pretraining
- fine-tuning / task tuning / post-training adaptation
- instruction tuning / instruction fine-tuning / SFT
- human feedback training / RLHF / reinforcement learning from human feedback
- model serving / inference serving / model hosting
- local model / self-hosted model / on-premises model
- open-weight model / openly available weights
- closed model / proprietary model
- model size / parameter count / model scale

## Do Not Confuse Candidates

| Candidate A | Candidate B | Difference to preserve |
|---|---|---|
| LLM | ChatGPT | An LLM is a type of language model; ChatGPT is a product that can combine models with policies, interface, and operations. |
| LLM | Artificial Intelligence | An LLM is one model type for language; Artificial Intelligence is the broader field. |
| LLM | Search engine | An LLM generates text from learned patterns; a search engine retrieves existing information. |
| generation | retrieval | Generation creates a new token sequence; retrieval finds existing information from a source. |
| model | product | A model is one computational component; a product includes the user experience and surrounding systems. |
| token | word | A token is the model's processing unit and may be a whole word, part of a word, punctuation, or a special symbol. |
| tokenization | encoding | Tokenization splits text into model units; encoding can refer more broadly to representing information numerically. |
| token | parameter | A token is input or output data; a parameter is a learned value inside the model. |
| prompt | context window | A prompt is supplied input or instruction; the context window is the total token capacity available to the model. |
| context | memory | Context is information available in the current sequence; memory is information deliberately stored for later use. |
| probability | score | A probability is normalized as a likelihood; a score or logit may be an unnormalized comparison value. |
| next-token prediction | next-word prediction | Models predict tokens, which do not always align with whole words. |
| decoding | sampling | Decoding is the overall conversion from scores to output tokens; sampling is one way to choose among candidates. |
| greedy decoding | sampling | Greedy decoding always selects the top candidate; sampling can select other candidates according to probabilities. |
| training | inference | Training updates parameters; inference uses fixed trained parameters to produce outputs. |
| pretraining | fine-tuning | Pretraining builds broad capabilities; fine-tuning adapts an existing model to a task or behavior. |
| fine-tuning | prompt engineering | Fine-tuning changes model parameters; prompt engineering changes the input instructions. |
| foundation model | LLM | Foundation model is a broad reusable model category; an LLM is specifically a language-focused model. |
| base model | chat model | A base model is usually pretrained before conversational alignment; a chat model is adapted for dialogue. |
| open-weight model | open-source model | Open weights do not necessarily mean all training data, code, or licensing is open. |
| local model | cloud model | A local model runs on user-controlled hardware; a cloud model is hosted by a remote provider. |
| parameter | activation | A parameter is learned and persistent; an activation is an intermediate value computed for a particular input. |
| attention | retrieval | Attention selects and mixes information within model context; retrieval fetches information from an external source. |
| context window | memory capacity | A context window is the model's per-request token view; memory capacity may refer to hardware or stored application data. |
| hallucination | random output | A hallucination is an unsupported or incorrect claim; randomness only describes how output variation is produced. |
| safety control | model capability | Safety controls constrain behavior around a model; capabilities describe what the model can do. |
| API | model | An API is an access interface; the model is the computational system behind the interface. |
| latency | throughput | Latency measures time for a request; throughput measures work completed per unit of time. |
| accuracy | perplexity | Accuracy counts task correctness; perplexity measures uncertainty in token prediction and is not a complete quality score. |
| generated reply | verified fact list | A generated reply is composed from learned patterns and context; it is not automatically a database of guaranteed facts. |

## Notes

- Primary extraction source: the complete body of `llm.html`, including the definition, analogy, five-step process flow, three real-world examples, misconception cards, related-concept map, remember-this statement, and video metadata.
- The page's central definition is that an LLM is a model trained to work with language tokens.
- The page explicitly describes statistical patterns in large text datasets, likely next-token prediction, probabilities, Transformer layers, a repeated generation loop, stopping conditions, and decoding into readable text.
- The page explicitly uses the autocomplete analogy, but the analogy is not a guarantee that the model retrieves a fact list from a database.
- The page explicitly distinguishes LLM from ChatGPT, Artificial Intelligence, and a search engine.
- The related-concept map explicitly connects Text → Tokens → Transformer → Next-token loop → Text.
- The page explicitly says an AI product may add retrieval, tools, memory, and safety controls around the model.
- Candidates in the main table intentionally retain both core terms and directly related technical, product, evaluation, safety, and deployment vocabulary; aliases and near-duplicates are not consolidated.
- Concepts listed under Potential Missing Concepts are useful glossary candidates that are not fully explained in the page body and should be verified before being treated as page-derived definitions.
- The video metadata names tokens, Transformer, and next-token prediction; the video itself was not used as a separate source for this extraction.
