# Topic

Ollama

## Topic Metadata

- Module: 07 · Model Serving & Local AI
- Topic: Ollama
- Topic Number: 01
- Source Page Context: 07 · Model Serving & Local AI · Topic 01 (as shown in the HTML)
- Source File: `ollama.html`
- Source Title: What is Ollama?
- Source Description: Ollama is software that helps you download, run, and manage AI models locally.
- Raw-stage policy: Maximum candidate inventory; retain overlapping, repeated, surface-form, alias, and potentially confusable candidates for later normalization.

## Glossary Candidates

| English Term | 中文 | Simple English | 中文小白理解 |
|---|---|---|---|
| Ollama | Ollama；奥拉玛 | Software for downloading, running, and managing AI models locally. | 用来在本地下载、运行和管理 AI 模型的软件。 |
| What is Ollama? | 什么是 Ollama？ | The page's question about Ollama's role. | 页面用来解释 Ollama 是什么的问题。 |
| ollama | ollama；Ollama 的小写写法 | Lowercase spelling used in commands and technical contexts. | 命令或技术语境中使用的小写写法。 |
| local AI | 本地 AI | AI that runs on the user's own computer or local environment. | 在自己的电脑或本地环境中运行的 AI。 |
| Local AI | 本地 AI | Title-case wording for AI running locally. | “本地 AI”的标题式写法。 |
| model serving | 模型服务；模型提供 | Making a model available to receive requests and return inference results. | 让模型能接收请求并返回推理结果。 |
| Model Serving & Local AI | 模型服务与本地 AI | The parent topic grouping for this page. | 这个页面所属的“模型服务与本地 AI”主题。 |
| software | 软件 | A program that provides Ollama's local model capabilities. | 提供 Ollama 本地模型能力的程序。 |
| local software | 本地软件 | Software installed and operated on a local computer. | 安装并在本地电脑上运行的软件。 |
| runtime | 运行时 | Software that loads and runs models. | 负责加载和运行模型的软件环境。 |
| model runtime | 模型运行时 | A runtime built around AI models. | 围绕 AI 模型、负责实际运行的运行时。 |
| Ollama runtime | Ollama 运行时 | The Ollama component that loads and manages the selected model. | Ollama 中加载和管理所选模型的组件。 |
| runtime around AI models | 围绕 AI 模型的运行时 | A runtime layer that operates AI models. | 包在 AI 模型外面、负责运行它们的软件层。 |
| AI model | AI 模型 | A learned computational model that can be run to generate results. | 可以被运行并生成结果的学习型计算模型。 |
| AI models | AI 模型（复数） | Multiple AI models that can be downloaded, loaded, or managed. | 可以下载、加载或管理的多个 AI 模型。 |
| model | 模型 | The model selected for loading and inference. | 被选中并加载来执行推理的模型。 |
| models | 模型（复数） | More than one model installed or managed by Ollama. | Ollama 安装或管理的多个模型。 |
| selected model | 所选模型 | The model chosen for a particular request. | 针对某次请求选择的模型。 |
| Selected Model | 所选模型 | The process-diagram label for the chosen model. | 流程图中表示被选模型的标签。 |
| multiple models | 多个模型 | Several models installed on one computer. | 同一台电脑上安装的多个模型。 |
| model package | 模型包 | A package containing model material that can be downloaded. | 可以下载、包含模型内容的打包文件。 |
| model packages | 模型包（复数） | Downloadable packages for AI models. | 可下载的多个 AI 模型包。 |
| model file | 模型文件 | A file containing model data used by the runtime. | 运行时会使用的模型数据文件。 |
| model files | 模型文件（复数） | The files that Ollama loads to run a model. | Ollama 加载来运行模型的多个文件。 |
| model file / weights | 模型文件／权重 | The model file or learned weights containing what the model knows. | 含有模型所学内容的模型文件或权重。 |
| weights | 权重 | Learned numerical data that represents what a model knows. | 表示模型所学知识的数值数据。 |
| model weights | 模型权重 | Learned numerical data of an AI model. | AI 模型经过学习得到的数值数据。 |
| learned data | 学习到的数据 | Data learned during model training. | 模型训练过程中学到的数据表示。 |
| learned numerical data | 学到的数值数据 | Numerical values that encode learned model behavior or knowledge. | 编码模型行为或知识的数值。 |
| what the model knows | 模型知道的内容 | An informal description of information represented in model weights. | 对模型权重里所表示信息的通俗说法。 |
| download | 下载 | Obtain a model package on the local computer. | 把模型包拿到本地电脑上。 |
| Download | 下载 | The capability label for obtaining model packages. | 能力列表中表示获取模型包的标签。 |
| downloading models | 下载模型 | Getting AI model packages for local use. | 获取 AI 模型包以便在本地使用。 |
| load | 加载 | Read a model file into the runtime so it can be used. | 把模型文件读入运行时以便使用。 |
| Load | 加载 | The capability label for loading model files. | 能力列表中表示加载模型文件的标签。 |
| loading a model | 加载模型 | Bringing a selected model into the runtime. | 把所选模型放进运行时。 |
| run | 运行 | Operate a loaded model to process a request. | 让已加载的模型处理请求。 |
| Run | 运行 | The capability label for running inference. | 能力列表中表示执行推理的标签。 |
| running a model | 运行模型 | Using a loaded model to produce a response. | 使用已加载模型生成响应。 |
| manage | 管理 | Control model versions or configurations and operate installed models. | 管理模型版本、配置和已安装模型。 |
| Manage | 管理 | The capability label for managing versions and configuration. | 能力列表中表示管理版本和配置的标签。 |
| model management | 模型管理 | Handling installed models, versions, and configurations. | 处理已安装模型、版本和配置。 |
| model versions | 模型版本 | Different versions of a model that can be managed locally. | 可以在本地管理的模型不同版本。 |
| version | 版本 | A particular release or variant of a model package. | 模型包的某个发布版本或变体。 |
| versions & config | 版本与配置 | The page's shorthand for model versions and configuration. | 页面中“模型版本和配置”的简写。 |
| configuration | 配置 | Settings that determine how a model or runtime is managed or used. | 决定模型或运行时如何管理、使用的设置。 |
| config | 配置；config 的缩写 | Short form of configuration. | configuration 的常见缩写。 |
| local model runtime | 本地模型运行时 | Runtime software that runs models on a local computer. | 在本地电脑上运行模型的软件运行时。 |
| local model | 本地模型 | A model available or running on the local computer. | 在本地电脑上可用或运行的模型。 |
| model inference | 模型推理 | Running a trained model to produce an output. | 运行训练好的模型来产生输出。 |
| inference | 推理 | The computation that generates a response from a model. | 模型根据输入计算并生成响应的过程。 |
| Inference | 推理 | The process-diagram label for model response generation. | 流程图中表示模型生成响应的标签。 |
| run inference | 执行推理 | Use a model to calculate and generate a result. | 使用模型计算并生成结果。 |
| generated response | 生成的响应 | The response produced by a model after inference. | 模型推理后生成的回答。 |
| response | 响应；回答 | Output sent back after a request is processed. | 请求处理后返回的输出或回答。 |
| generate a response | 生成响应 | Produce an answer using the selected model and hardware. | 用所选模型和硬件生成回答。 |
| local API | 本地 API | A local interface through which applications send requests to Ollama. | 本地应用向 Ollama 发送请求的接口。 |
| API | API；应用程序编程接口 | An interface that receives requests from an application. | 接收应用请求的软件接口。 |
| Ollama API | Ollama API | Ollama's local request interface. | Ollama 提供的本地请求接口。 |
| interface | 接口 | A boundary through which a local app communicates with Ollama. | 本地应用和 Ollama 交流的边界。 |
| local interface | 本地接口 | An interface running on and reachable from the local computer. | 在本地电脑上运行、可被本地程序访问的接口。 |
| request | 请求 | A message sent by a person or application for Ollama to process. | 人或应用发给 Ollama、要求它处理的消息。 |
| local request | 本地请求 | A request sent within the local computer or environment. | 在本地电脑或环境中发出的请求。 |
| user | 用户 | A person who sends a request to a local AI application or runtime. | 向本地 AI 应用或运行时发送请求的人。 |
| User | 用户 | The process-flow label for a person initiating a request. | 流程图中表示发起请求的人的标签。 |
| application | 应用程序；应用 | Software that sends requests to the Ollama API. | 向 Ollama API 发送请求的软件。 |
| User / Application | 用户／应用程序 | The process node representing either a person or local app. | 流程节点中表示用户或本地应用的合并写法。 |
| local app | 本地应用 | An application running on the same computer or local environment. | 在同一台电脑或本地环境中运行的应用。 |
| local application | 本地应用程序 | An app that calls Ollama locally. | 在本地调用 Ollama 的应用程序。 |
| local web app | 本地 Web 应用 | A locally running web application that calls Ollama's API. | 在本地运行并调用 Ollama API 的网页应用。 |
| app | 应用；app 的缩写 | Short form of application. | application 的常见缩写。 |
| caller | 调用方 | A user or application that sends a request to Ollama. | 向 Ollama 发送请求的用户或应用。 |
| call the API | 调用 API | Send a request through the API interface. | 通过 API 接口发送请求。 |
| calls Ollama's API | 调用 Ollama 的 API | A local application sends a request through Ollama's API. | 本地应用通过 Ollama API 发请求。 |
| receive a request | 接收请求 | Accept an incoming request at the local interface. | 本地接口收到传入请求。 |
| receives the request | 接收该请求 | The Ollama API gets the request from a user or app. | Ollama API 从用户或应用收到请求。 |
| selected model → hardware → inference | 所选模型→硬件→推理 | The page's simplified execution chain. | 页面用来表示模型借助硬件执行推理的链条。 |
| hardware | 硬件 | Computer resources used by the model to generate a response. | 模型生成回答时使用的电脑硬件资源。 |
| available computer hardware | 可用的计算机硬件 | The hardware resources available on the local machine. | 本地机器上能够使用的硬件资源。 |
| computer | 计算机；电脑 | The local machine where Ollama and models can run. | Ollama 和模型可以运行的本地机器。 |
| local computer | 本地电脑 | The user's own computer hosting the runtime and models. | 承载运行时和模型的用户自己的电脑。 |
| generate | 生成 | Produce a response from an inference computation. | 通过推理计算产生回答。 |
| generated output | 生成的输出 | Output produced by the selected model. | 所选模型产生的输出。 |
| output | 输出 | The result returned after model inference or an application call. | 模型推理或应用调用后返回的结果。 |
| result | 结果 | The model output or response sent back to the requester. | 返回给请求方的模型输出或响应。 |
| send back the result | 发回结果 | Return the result to the local application or user. | 把结果返回给本地应用或用户。 |
| return a response | 返回响应 | Send the generated response back after processing. | 处理完成后把生成的响应发回去。 |
| runtime flow | 运行时流程 | The path from request through API and runtime to inference. | 从请求经 API、运行时到推理的路径。 |
| execution flow | 执行流程 | The sequence in which the selected model uses hardware to infer a response. | 所选模型借助硬件完成推理的顺序。 |
| under the hood | 底层原理；幕后运行方式 | A beginner-friendly label for the internal process. | 用来说明内部如何运行的通俗说法。 |
| How it works | 工作原理 | The section explaining Ollama's request-to-inference flow. | 解释 Ollama 从请求到推理流程的部分。 |
| process step | 流程步骤 | One numbered stage in the execution diagram. | 执行流程图中的一个编号阶段。 |
| process node | 流程节点 | A named component or stage in the diagram. | 图中表示组件或阶段的一个节点。 |
| local model serving | 本地模型服务 | Serving model inference through a local runtime and interface. | 通过本地运行时和接口提供模型推理。 |
| model loading | 模型加载 | Bringing model files into the runtime. | 把模型文件载入运行时。 |
| model switching | 模型切换 | Moving between installed models as needed. | 根据需要在已安装模型之间切换。 |
| installed model | 已安装模型 | A model package present on the local computer. | 已经放在本地电脑上的模型。 |
| installed models | 已安装模型（复数） | Several model packages present on one computer. | 同一台电脑上存在的多个模型包。 |
| install | 安装 | Put a model package on the local computer for use. | 把模型包放到本地电脑上以便使用。 |
| setup | 设置；配置 | The local arrangement in which models are installed. | 模型安装在电脑上的本地配置状态。 |
| one computer | 一台电脑 | The single local machine hosting several models in the example. | 例子中安装多个模型的同一台电脑。 |
| as needed | 按需 | Switching to a model when a task requires it. | 任务需要时再切换到相应模型。 |
| ollama pull | ollama pull；拉取模型 | A command shown for downloading a model. | 页面展示的用于下载模型的命令。 |
| pull | 拉取 | Download a model package into the local environment. | 把模型包下载到本地环境。 |
| ollama pull → download | ollama pull→下载 | The page's command-to-action mapping for model download. | 页面把 ollama pull 映射为下载动作。 |
| ollama run | ollama run；运行模型 | A command shown for loading and running a model. | 页面展示的用于加载并运行模型的命令。 |
| ollama run → load + run | ollama run→加载+运行 | The page's command-to-action mapping for loading and running. | 页面把 ollama run 映射为加载并运行。 |
| command | 命令 | A textual instruction such as `ollama pull` or `ollama run`. | 例如 `ollama pull` 或 `ollama run` 这样的文字指令。 |
| command-line command | 命令行命令 | A command typed to control Ollama locally. | 在本地命令行中控制 Ollama 的指令。 |
| pull command | 拉取命令 | The `ollama pull` command used to download a model. | 用于下载模型的 `ollama pull` 指令。 |
| run command | 运行命令 | The `ollama run` command used to load and run a model. | 用于加载并运行模型的 `ollama run` 指令。 |
| load + run | 加载并运行 | The combined action represented by `ollama run`. | `ollama run` 所表示的两个连续动作。 |
| media player | 媒体播放器 | The analogy used for Ollama's relationship to a model file. | 页面用来类比 Ollama 的媒体播放器。 |
| Think of it like a media player | 把它想成媒体播放器 | The page's beginner-friendly analogy. | 页面帮助初学者理解 Ollama 的类比。 |
| movie file | 电影文件 | The analogy for an AI model file or weights. | 类比中对应 AI 模型文件或权重的电影文件。 |
| file | 文件 | A stored model artifact that can be loaded by software. | 软件可以加载的已存储模型内容。 |
| player | 播放器 | The analogy for software that loads and uses a file. | 类比中表示加载并使用文件的软件。 |
| AI model as movie file | AI 模型像电影文件 | The model is the content, while Ollama is the player. | 模型像内容文件，Ollama 像播放它的播放器。 |
| Ollama as media player | Ollama 像媒体播放器 | Ollama loads and runs the model file like a player handles media. | Ollama 像播放器一样加载并运行模型文件。 |
| model-content distinction | 模型与运行软件的区分 | The distinction between learned model content and runtime software. | 区分模型本身的内容和运行模型的软件。 |
| runtime-software distinction | 运行时软件区分 | The boundary between Ollama and the model it runs. | Ollama 与它所运行模型之间的边界。 |
| Llama | Llama | One family or example of models Ollama can run. | Ollama 可以运行的模型家族或示例之一。 |
| Qwen | Qwen；通义千问模型家族 | One model family named as an example on the page. | 页面列出的模型家族示例之一。 |
| DeepSeek | DeepSeek；深度求索模型家族 | One model family named as an example on the page. | 页面列出的模型家族示例之一。 |
| Mistral | Mistral | One model family named as an example on the page. | 页面列出的模型家族示例之一。 |
| more models | 更多模型 | Other models beyond Llama, Qwen, DeepSeek, and Mistral. | 除列举模型外的其他模型。 |
| Llama, Qwen, DeepSeek, Mistral | Llama、Qwen、DeepSeek、Mistral | The page's list of example model families. | 页面列出的四个模型示例名称。 |
| local chat | 本地聊天 | A chat interaction with a model running locally. | 和本地运行的模型进行聊天。 |
| Local Chat | 本地聊天 | The heading for the first real-world example. | 第一个现实例子的标题。 |
| chat | 聊天 | An interactive exchange with a language model. | 与语言模型进行的互动交流。 |
| prompt | 提示词；提示 | Text sent to a model to request a response. | 发给模型、要求它回答的文字。 |
| Prompt | 提示词；提示 | The label for the input in the local-chat example. | 本地聊天例子中的输入标签。 |
| “Explain machine learning.” | “解释机器学习。” | The example prompt sent to a locally running model. | 例子中要求模型解释机器学习的提示。 |
| explain machine learning | 解释机器学习 | The task requested by the example prompt. | 示例提示要求完成的任务。 |
| Qwen model | Qwen 模型 | The model loaded in the local-chat example. | 本地聊天例子中被加载的 Qwen 模型。 |
| loads a Qwen model | 加载 Qwen 模型 | Ollama loads Qwen to answer the example prompt. | Ollama 加载 Qwen 来回答示例问题。 |
| returns its generated response | 返回其生成的响应 | Ollama sends back the response produced by Qwen. | Ollama 返回 Qwen 生成的回答。 |
| real-world example | 现实世界例子 | An example showing how Ollama is used in practice. | 说明 Ollama 实际使用方式的例子。 |
| Real-world examples | 现实世界例子（复数） | The section containing local chat, local app, and multiple-model examples. | 包含三个实际使用例子的部分。 |
| local application example | 本地应用例子 | An example in which a local web app calls Ollama's API. | 本地网页应用调用 Ollama API 的例子。 |
| Local Application | 本地应用 | The heading for the second real-world example. | 第二个现实例子的标题。 |
| request from a local app | 来自本地应用的请求 | A request sent by a local web application to Ollama. | 本地网页应用发送给 Ollama 的请求。 |
| selected model in an app | 应用中的所选模型 | The model the local app asks Ollama to run. | 本地应用请求 Ollama 运行的模型。 |
| sends back the result | 发回结果 | Ollama returns the model result to the application. | Ollama 把模型结果返回给应用。 |
| multiple-model example | 多模型例子 | An example with several installed models and model switching. | 有多个已安装模型并需要切换的例子。 |
| Multiple Models | 多个模型 | The heading for the third real-world example. | 第三个现实例子的标题。 |
| several models | 多个模型；若干模型 | More than one model installed on a computer. | 一台电脑上安装的不止一个模型。 |
| switch between models | 在模型之间切换 | Choose a different installed model as needed. | 按需要选择另一个已安装模型。 |
| model selection | 模型选择 | Choosing which installed model should handle a request. | 选择哪个已安装模型处理请求。 |
| Ollama ≠ LLM | Ollama 不等于 LLM | Ollama runs and manages models; an LLM is the language model itself. | Ollama 是运行和管理软件，LLM 才是语言模型本身。 |
| LLM | 大语言模型 | The actual language model, contrasted with Ollama. | 和 Ollama 区分开的真正语言模型。 |
| large language model | 大语言模型 | A language model that produces language outputs. | 能生成语言输出的语言模型。 |
| language model | 语言模型 | The model that generates language, as distinct from the runtime. | 负责生成语言、不同于运行时的软件模型。 |
| actual language model | 真正的语言模型 | The model itself rather than the software that runs it. | 模型本身，而不是运行模型的软件。 |
| Ollama runs models | Ollama 运行模型 | Ollama provides the software layer that operates models. | Ollama 提供实际运行模型的软件层。 |
| Ollama manages models | Ollama 管理模型 | Ollama handles model packages, versions, configurations, and selection. | Ollama 处理模型包、版本、配置和选择。 |
| Ollama ≠ Model Weights | Ollama 不等于模型权重 | Ollama is runtime software, while weights are learned numerical data. | Ollama 是运行软件，权重是学到的数值数据。 |
| runtime software | 运行时软件 | Software used to load and run a model. | 用来加载和运行模型的软件。 |
| learned numerical data of the model | 模型学到的数值数据 | Numerical data representing what the model learned. | 表示模型学习结果的数值数据。 |
| Ollama ≠ AI Agent | Ollama 不等于 AI 智能体 | Ollama provides inference; an agent is a larger system with tools and a control loop. | Ollama 提供推理，智能体还包括工具和控制循环。 |
| AI Agent | AI 智能体 | A larger system containing a model, tools, and a control loop. | 由模型、工具和控制循环组成的更大系统。 |
| agent | 智能体 | Short form of AI agent. | AI agent 的简称。 |
| larger system | 更大的系统 | The broader system that can include a model, tools, and a loop. | 可以包含模型、工具和循环的更大整体。 |
| tools | 工具 | External capabilities available to an agent. | 智能体可以使用的外部能力。 |
| control loop | 控制循环 | A loop that coordinates model actions and tool or system steps. | 协调模型动作及工具或系统步骤的循环。 |
| model, tools, and a control loop | 模型、工具和控制循环 | The three elements named in the page's agent comparison. | 页面比较智能体时列出的三个组成部分。 |
| model inference vs agent | 模型推理与智能体 | The boundary between running a model and operating a larger agent system. | 区分单纯运行模型和运行更大的智能体系统。 |
| Ollama ≠ ChatGPT | Ollama 不等于 ChatGPT | Ollama is a local model runtime; ChatGPT is a complete AI product and service. | Ollama 是本地模型运行时，ChatGPT 是完整 AI 产品和服务。 |
| ChatGPT | ChatGPT | A complete AI product and service contrasted with Ollama. | 和 Ollama 对比的完整 AI 产品和服务。 |
| complete AI product and service | 完整 AI 产品和服务 | A full user-facing product and service, not merely a local runtime. | 面向用户的完整产品和服务，而不只是本地运行时。 |
| product | 产品 | ChatGPT's product-level offering in the comparison. | 对比中 ChatGPT 所代表的产品层。 |
| service | 服务 | ChatGPT's service-level offering in the comparison. | 对比中 ChatGPT 所代表的服务层。 |
| local model runtime vs AI product | 本地模型运行时与 AI 产品 | The distinction between Ollama and ChatGPT. | Ollama 与 ChatGPT 之间的产品边界。 |
| What it is NOT | 它不是什么 | The section clarifying what Ollama should not be confused with. | 专门说明不应把 Ollama 和什么混淆的部分。 |
| not a model | 不是模型 | Ollama is software that runs models rather than a model itself. | Ollama 是运行模型的软件，不是模型本身。 |
| not model weights | 不是模型权重 | Ollama is not the learned numerical data inside a model. | Ollama 不是模型内部学到的数值数据。 |
| not an agent | 不是智能体 | Ollama alone is not the larger model-tools-control-loop system. | Ollama 单独并不是模型、工具和控制循环组成的智能体。 |
| not ChatGPT | 不是 ChatGPT | Ollama is not the complete ChatGPT product and service. | Ollama 不是完整的 ChatGPT 产品和服务。 |
| related concepts | 相关概念 | Concepts connected to Ollama in the page's placement map. | 页面关系图中和 Ollama 相连的概念。 |
| application → Ollama → model → quantization → hardware → inference | 应用→Ollama→模型→量化→硬件→推理 | The page's simplified relationship chain. | 页面展示的从应用到推理的概念链。 |
| application → Ollama | 应用→Ollama | An application sends work to the local Ollama runtime. | 应用把工作交给本地 Ollama 运行时。 |
| Ollama → model | Ollama→模型 | Ollama loads or manages the selected model. | Ollama 加载或管理所选模型。 |
| model → quantization | 模型→量化 | The model may be represented or prepared through quantization. | 模型可能通过量化得到适合运行的表示。 |
| quantization | 量化 | A model-compression or numerical-representation technique linked to local serving. | 与本地运行相关的模型压缩或数值表示技术。 |
| model → hardware | 模型→硬件 | The model uses available hardware during execution. | 模型运行时使用可用硬件。 |
| hardware → inference | 硬件→推理 | Hardware supports the computation that produces inference. | 硬件支持生成推理结果的计算。 |
| Runtime Constraints | 运行时约束 | A related topic about limits affecting local model execution. | 会影响本地模型运行的限制这一相关主题。 |
| Context Window | 上下文窗口 | A related topic about how much context a model can process. | 模型一次能处理多少上下文的相关主题。 |
| related topic | 相关主题 | A linked topic for exploring Ollama's surrounding concepts. | 可用来继续学习 Ollama 周边概念的主题。 |
| Explore next | 接下来探索 | The navigation prompt for related topics. | 引导读者继续查看相关主题的提示。 |
| remember this | 记住这一点 | The page's final takeaway section. | 页面最后总结核心观点的部分。 |
| Ollama is software for running and managing AI models locally | Ollama 是用于在本地运行和管理 AI 模型的软件 | The page's concise final takeaway. | 页面对 Ollama 的最终简明总结。 |
| local model management software | 本地模型管理软件 | Software that runs and manages AI models on a local computer. | 在本地电脑上运行和管理 AI 模型的软件。 |
| beginner-friendly mental model | 适合初学者的心智模型 | A simple analogy used to explain the relationship between runtime and model. | 用简单类比解释运行时和模型关系的理解框架。 |
| mental model | 心智模型 | A simplified way to think about how Ollama and model files relate. | 帮助理解 Ollama 和模型文件关系的简化想法。 |
| short answer | 简短答案 | The first section that gives Ollama's core definition. | 页面最开始给出核心定义的部分。 |
| definition | 定义 | A statement of Ollama's role as a runtime around models. | 说明 Ollama 是模型运行时的陈述。 |
| capability | 能力 | A function Ollama provides, such as download, load, run, expose, or manage. | Ollama 提供的功能，例如下载、加载、运行、暴露和管理。 |
| capabilities | 能力集合 | The five listed Ollama capabilities. | 页面列出的五项 Ollama 能力。 |
| download, load, run, expose, manage | 下载、加载、运行、暴露、管理 | The page's five capability labels. | 页面列出的五个能力动作。 |
| expose | 暴露；提供 | Make a local API available to applications. | 让本地 API 可以被应用访问。 |
| expose a local API | 暴露本地 API | Make Ollama's local interface available for requests. | 提供可供应用调用的 Ollama 本地接口。 |
| expose vs publish | 暴露与发布 | Expose here means make a local interface available, not publish a public service. | 这里的暴露是提供本地接口，不等于发布公网服务。 |
| manage versions and configuration | 管理版本和配置 | Control the model versions and settings used by Ollama. | 管理 Ollama 使用的模型版本和设置。 |
| model package download | 模型包下载 | Obtaining a model package through Ollama. | 通过 Ollama 获取模型包。 |
| local inference service | 本地推理服务 | A locally accessible service that runs models and returns outputs. | 在本地运行模型并返回输出的服务。 |
| local API request | 本地 API 请求 | A request sent to the local Ollama interface. | 发给 Ollama 本地接口的请求。 |
| request-response interaction | 请求-响应交互 | A user or app sends a request and receives generated output. | 用户或应用发送请求并收到生成输出的交互。 |
| model-serving layer | 模型服务层 | The software layer between applications and model execution. | 位于应用与模型执行之间的软件层。 |
| application layer | 应用层 | The user-facing or local-app side that sends requests. | 发出请求的用户侧或本地应用侧。 |
| model layer | 模型层 | The learned model selected for inference. | 被选择来进行推理的学习模型层。 |
| hardware layer | 硬件层 | The local compute resources supporting model execution. | 支持模型运行的本地计算资源层。 |
| inference layer | 推理层 | The computation that turns a request into a response. | 把请求转成响应的计算层。 |
| local deployment | 本地部署 | Operating the model runtime and model on the user's computer. | 把运行时和模型部署在用户自己的电脑上。 |
| locally | 在本地 | On the user's own computer or local environment rather than a remote service. | 在自己的电脑或本地环境，而不是远程服务上。 |
| remote service | 远程服务 | A service running outside the local computer, used as a contrast. | 与本地运行相对、在外部运行的服务。 |
| local-vs-cloud boundary | 本地与云端边界 | The distinction implied by the page's focus on local execution. | 页面强调本地运行时所暗示的本地与云端区别。 |
| independent explainer | 独立讲解内容 | A separate explanatory video associated with the topic. | 与主题配套的独立讲解视频内容。 |
| visual explainer | 可视化讲解 | Visual material explaining Ollama. | 用视觉内容解释 Ollama 的材料。 |
| video | 视频 | The media section containing the Ollama explainer. | 页面中包含 Ollama 讲解内容的媒体部分。 |
| captions | 字幕 | Text captions included with the explainer video. | 讲解视频中提供的文字字幕。 |
| English captions | 英文字幕 | English-language captions for the video. | 视频提供的英文文字字幕。 |
| video explanation of Ollama | Ollama 视频讲解 | A visual explanation of the topic. | 用视频方式解释 Ollama 的内容。 |

## Potential Missing Concepts

- The page does not define the exact Ollama installation process, supported operating systems, package manager, executable path, service startup, or upgrade/uninstall workflow.
- It mentions a local API but does not specify an endpoint, URL, port, HTTP method, request schema, response schema, streaming format, authentication model, or SDK.
- It does not name the precise model-file format, manifest format, model registry, repository, tag syntax, digest, cache location, storage layout, or pull metadata.
- It shows `ollama pull` and `ollama run` but does not document their full command syntax, model names, flags, interactive behavior, detached/background mode, or exit codes.
- It does not explain how models are discovered, listed, inspected, copied, removed, updated, tagged, or selected when several versions are installed.
- It does not distinguish a model family from a model variant, checkpoint, tag, quantized build, adapter, or custom model definition.
- It introduces weights but does not define tensors, parameters, checkpoints, tokenizer files, architecture metadata, or how weights are mapped into memory.
- It links quantization but does not explain precision, bit width, GGUF or other formats, quality trade-offs, memory savings, or quantization methods.
- It does not explain CPU inference, GPU inference, VRAM, unified memory, CPU/GPU offload, device selection, threading, batching, or accelerator support.
- It does not cover context length, context window allocation, KV cache, prompt processing, generation loop, sampling, temperature, top-p, stop sequences, or maximum output tokens.
- It does not define latency, time to first token, tokens per second, throughput, concurrency, memory footprint, startup time, or other serving metrics.
- It does not explain whether the runtime keeps models loaded, unloads them, queues requests, supports parallel requests, or reuses a loaded model.
- It does not document local networking, bind addresses, cross-origin access, reverse proxies, TLS, firewall rules, or exposing the API beyond the local machine.
- It does not discuss privacy, data retention, telemetry, logs, prompt storage, model downloads, licensing, or whether requests leave the device.
- It does not explain model safety, content moderation, guardrails, prompt injection, or the difference between local execution and safe output.
- It does not define the relationship between Ollama and an LLM architecture, tokenizer, transformer, inference engine, or backend implementation.
- It does not explain how Ollama relates to other local runtimes, model servers, desktop clients, cloud model APIs, or hosted AI products.
- It does not provide a concrete API request/response example, a curl example, a code sample, a configuration file, or a troubleshooting procedure.
- It does not specify how local applications authenticate or authorize access to the API, or how multiple users and tenants would be isolated.
- It does not define model compatibility, hardware minimums, operating-system requirements, supported architectures, or failure behavior for insufficient memory.
- It does not describe download progress, interrupted downloads, retries, checksums, integrity verification, resumability, or offline installation.
- It does not explain how a Qwen model is chosen for the chat example or how task-specific model selection should work.
- It does not define “what the model knows” beyond the beginner-facing analogy to learned weights.
- It does not explain whether “manage configurations” includes prompt templates, system prompts, runtime parameters, Modelfiles, environment variables, or per-request options.
- It does not describe persistence, data volume management, disk usage, cache eviction, backups, or model portability.
- It does not provide quantitative comparisons between local and remote inference or between different models and quantization levels.
- It does not define the exact boundary between Ollama runtime software and an application that provides chat UI, memory, tools, agents, or workflows.
- It links AI Agent, Runtime Constraints, LLM, and Context Window, but these related concepts are not defined in detail on this page.
- It contains a video and captions but does not add concepts from the video beyond the page's visible caption and media labels.

## Aliases / Synonyms

- Ollama / ollama / Ollama runtime / local model runtime / local model serving software / model runtime
- local AI / Local AI / AI running locally / locally running AI / local model execution
- software / local software / runtime software / serving software / model-serving layer
- AI model / AI models / model / models / selected model / installed model / installed models
- model package / model packages / model file / model files / model file / weights / model weights
- learned data / learned numerical data / learned numerical data of the model / what the model knows
- download / Download / downloading models / model package download / `ollama pull` / pull / pull command
- load / Load / loading a model / model loading / `ollama run` / load + run
- run / Run / running a model / run inference / model inference / inference / Inference
- manage / Manage / model management / manage versions and configuration / versions & config / config
- model versions / version / configuration / config / settings
- local API / Ollama API / API / interface / local interface / expose a local API / expose
- user / User / application / app / local app / local application / local web app / User / Application
- request / local request / local API request / request from a local app / incoming request
- response / generated response / generated output / output / result / returned result
- send back the result / sends back the result / return a response / returns its generated response
- selected model → hardware → inference / model → hardware → inference / execution flow / runtime flow
- hardware / available computer hardware / computer / local computer / local machine
- model switching / switch between models / model selection / as needed
- media player / player / movie file / file / Think of it like a media player
- Llama / Qwen / DeepSeek / Mistral / more models / model families
- local chat / Local Chat / chat / prompt / Prompt / “Explain machine learning.”
- Qwen model / loads a Qwen model / returns its generated response
- local application example / Local Application / local web app / calls Ollama's API
- multiple-model example / Multiple Models / several models / switch between models
- LLM / large language model / language model / actual language model
- AI Agent / agent / larger system / model, tools, and a control loop
- tools / external capabilities / control loop / agent loop (adjacent concept)
- ChatGPT / complete AI product and service / AI product / AI service
- related concepts / related topic / Explore next / relationship chain
- quantization / model quantization (adjacent wording) / model → quantization
- Runtime Constraints / runtime constraints (title-case and lowercase variants)
- Context Window / context window (title-case and lowercase variants)
- short answer / definition / core definition / What is it?
- capability / capabilities / download, load, run, expose, manage
- independent explainer / visual explainer / video / video explanation of Ollama
- captions / English captions

## Do Not Confuse Candidates

- Ollama ≠ AI model: Ollama is runtime software; an AI model is the learned computational artifact it loads and runs.
- Ollama ≠ LLM: Ollama runs and manages models; an LLM is the actual language model.
- Ollama ≠ model weights: Ollama is software; weights are learned numerical data inside or associated with a model.
- Ollama ≠ model file: Ollama loads a model file; the file is not the runtime itself.
- Ollama ≠ model package: a package is downloadable model material; Ollama is the software that manages and runs it.
- Ollama ≠ inference: Ollama provides the runtime around inference; inference is the computation that generates a response.
- Ollama ≠ local API: Ollama exposes or provides a local API; the API is the interface, not the whole runtime.
- Ollama ≠ application: an application sends requests to Ollama; Ollama is the local model runtime it calls.
- Ollama ≠ local web app: a local web app may call Ollama's API, but it is a separate application layer.
- Ollama ≠ hardware: hardware supplies compute resources; Ollama is the software using them.
- Ollama ≠ quantization: quantization is a model representation or compression technique; Ollama is the runtime managing the model.
- Ollama ≠ AI Agent: an agent is a larger system with a model, tools, and a control loop; Ollama alone provides model inference.
- Ollama ≠ tools: tools are capabilities an agent may use; Ollama is not automatically an agent tool or control loop.
- Ollama ≠ control loop: a control loop coordinates repeated system steps; Ollama is the model runtime.
- Ollama ≠ ChatGPT: Ollama is a local model runtime; ChatGPT is a complete AI product and service.
- model ≠ model weights: a model can refer to the complete runnable artifact or conceptual model; weights are its learned numerical data.
- model file ≠ model package: a model file is a file artifact; a package may bundle files and metadata for download and management.
- model package ≠ model runtime: the package is content to obtain; the runtime is software that loads and runs it.
- model weights ≠ model runtime: weights encode learned behavior; runtime software performs the loading and computation.
- selected model ≠ all installed models: the selected model is the one chosen for a request, while several models may be installed.
- model family ≠ individual model variant: Llama, Qwen, DeepSeek, and Mistral are named examples or families; the page does not define their variants.
- download ≠ load: downloading obtains the model package; loading brings model data into the runtime for use.
- load ≠ run: loading prepares the model; running performs the model operation and inference.
- run ≠ run inference: “run” is a broad capability label; “run inference” names the specific response-generating computation.
- install ≠ load: an installed model is present on disk; a loaded model is available in the runtime.
- manage ≠ run: management includes versions and configuration; running executes a selected model for a request.
- expose ≠ publish publicly: exposing a local API makes it available to local applications; it does not imply a public cloud service.
- local API ≠ remote API: the page describes a local interface and does not claim that it is a hosted remote endpoint.
- API ≠ application: an API is an interface; an application is software that uses the interface.
- request ≠ response: a request goes into the runtime; a response or result comes back after processing.
- output ≠ model: output is generated content; the model is the artifact that generates it.
- generated response ≠ model weights: a response is run-time output; weights are learned data used to produce it.
- model selection ≠ inference: selecting chooses the model; inference runs it.
- hardware ≠ runtime: hardware provides physical compute; runtime software orchestrates model execution on it.
- hardware ≠ model: hardware is the machine resource; the model is the learned artifact.
- quantization ≠ inference: quantization changes or represents model values; inference uses the resulting model to generate output.
- media player ≠ movie file: in the analogy Ollama is the player and the model file or weights are the movie file.
- movie file ≠ learned weights literally: the movie-file language is an analogy, not a claim that weights are video media.
- local chat ≠ local model runtime: local chat is one use case; Ollama is the runtime enabling it.
- prompt ≠ response: “Explain machine learning.” is an input prompt; the generated explanation is the response.
- Qwen model ≠ Ollama: Qwen is the model loaded in the example; Ollama is the software loading it.
- multiple models ≠ multiple Ollama runtimes: several models can be installed on one computer without implying several runtime installations.
- switch between models ≠ retrain a model: switching chooses another installed model; the page does not describe training.
- LLM ≠ AI Agent: an LLM is a language model; an agent is a larger system that may include a model, tools, and a control loop.
- AI Agent ≠ Ollama: an agent may use a local runtime, but Ollama alone is not an agent.
- tools ≠ model weights: tools are external capabilities; weights are learned numerical data.
- control loop ≠ Ollama runtime: the loop coordinates actions; the runtime loads and runs models.
- ChatGPT ≠ local model runtime: ChatGPT is presented as a complete product and service, not simply local runtime software.
- complete AI product and service ≠ model: a product includes user-facing service layers beyond the underlying model.
- application → Ollama ≠ Ollama → model: the first relation is an app calling the runtime; the second is the runtime loading/managing a model.
- model → quantization ≠ quantization → hardware: the page's chain relates the concepts but does not state that quantization is hardware.
- related concept ≠ definition on this page: linked topics such as Runtime Constraints and Context Window are signposts, not fully explained concepts here.
- context window ≠ Ollama runtime: Context Window is a related model-capacity concept, not Ollama itself.
- runtime constraints ≠ model runtime: constraints limit execution; the runtime is the software performing execution.
- independent explainer ≠ runtime component: the video explains Ollama but does not run the model.
- captions ≠ model output: captions are explanatory media text, not a response generated by Ollama.

## Notes

- The source page was read in full, including the title, description, parent navigation, on-page navigation, short definition, five capability labels, model examples, media-player analogy, under-the-hood flow, command strip, three real-world examples, four “What it is NOT” comparisons, related-concepts chain, takeaway, and video section.
- The direct definition says: “Ollama is software that helps you download, run, and manage AI models locally.”
- The shorter definition says: “Ollama provides a runtime around AI models.”
- The page explicitly says Ollama can download model packages, load model files, run inference, expose a local API, and manage model versions or configurations.
- The named model examples are Llama, Qwen, DeepSeek, and Mistral, followed by “More”; the page does not define these model families in detail.
- The media-player analogy maps the AI model or model file / weights to a movie file and Ollama to the media player.
- The internal flow is User / Application → Ollama API → Ollama Runtime → Selected Model → Hardware → Inference.
- The user or local app sends a request; the local interface receives it; Ollama loads and manages the selected model; the model uses available computer hardware to generate a response.
- The command strip maps `ollama pull` to download and `ollama run` to load + run.
- The local-chat example uses the prompt “Explain machine learning.”, loads a Qwen model, and returns its generated response.
- The local-application example says a local web app calls Ollama’s API; Ollama runs the selected model and sends back the result.
- The multiple-model example says several models are installed on one computer and Ollama helps switch between them as needed.
- The page explicitly distinguishes Ollama from LLM, model weights, AI Agent, and ChatGPT.
- The related-concepts chain is Application → Ollama → Model → Quantization → Hardware → Inference.
- The page links Quantization, Runtime Constraints, LLM, and Context Window as next concepts; it does not explain their mechanics here.
- The final takeaway is: “Ollama is software for running and managing AI models locally.”
- The video is labeled an independent explainer and visual explainer with English captions; the page itself does not provide additional visible technical claims in that section.
- Candidate rows intentionally retain capitalization variants, singular/plural variants, repeated wording, command labels, beginner-facing phrases, source labels, and potentially confusable concepts for later normalization.
- Potential missing concepts are listed separately and are not treated as direct claims from this page.
- No website files, video assets, GitHub state, or other project files were modified.
