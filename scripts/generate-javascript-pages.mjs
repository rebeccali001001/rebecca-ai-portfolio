import { mkdirSync, writeFileSync } from 'node:fs';
import { resolve } from 'node:path';

const root = resolve(import.meta.dirname, '..');
const topics = [
  ['02','Data Types','data-types','Values have different kinds: text, numbers, true/false, and objects.','typeof value','name = "Rebecca"\nconst name = "Rebecca";','primitive vs object'],
  ['03','Operators','operators','Operators calculate, compare, assign, and combine values.','total + tax','total = price + tax\nconst total = price + tax;','===, ||, ??'],
  ['04','Conditions','conditions','Conditions choose which code runs based on a value.','A route with yes/no decisions.','if total > 100:\n    discount = True\nif (total > 100) {\n  discount = true;\n}','truthy and falsy'],
  ['05','Loops','loops','Loops repeat work over values or while a condition is true.','A checklist repeated for every item.','for item in items:\n    print(item)\nfor (const item of items) {\n  console.log(item);\n}','for...of and array methods'],
  ['06','Functions','functions','Functions package reusable behavior and can return a result.','A named recipe with inputs and an output.','def greet(name):\n    return f"Hi {name}"\nfunction greet(name) {\n  return `Hi ${name}`;\n}','declarations and arrow functions'],
  ['07','Scope','scope','Scope decides where a variable can be read or changed.','Rooms: a variable is available only in its room and nested rooms.','if active:\n    message = "Ready"\nif (active) {\n  const message = "Ready";\n}','let, const, var, closure'],
  ['08','Arrays','arrays','Arrays store an ordered list of values.','A numbered shelf of items.','items = ["Pen", "Book"]\nconst items = ["Pen", "Book"];','mutation and indexes'],
  ['09','Objects','objects','Objects group named properties that describe one thing.','A profile card with labeled fields.','user = {"name": "Rebecca"}\nconst user = { name: "Rebecca", role: "PM" };','properties and methods'],
  ['10','Strings','strings','Strings are text values, often used for labels, messages, and input.','A piece of text you can inspect and transform.','name = "Rebecca"\nconst name = "Rebecca";','template literals'],
  ['11','Numbers & Math','numbers-math','Numbers support calculation, conversion, and useful Math helpers.','A calculator with a few built-in buttons.','price = float("8.5")\nconst price = Number("8.5");','Number and Math'],
  ['12','Destructuring','destructuring','Destructuring takes values from an array or object into variables.','Unpacking a box directly into labeled places.','name = user["name"]\nconst { name, role } = user;','rename and defaults'],
  ['13','Spread & Rest','spread-rest','The same ... syntax can expand values or collect remaining values.','Spread opens a suitcase; rest packs leftovers into one.','all_items = [*a, *b]\nconst allItems = [...a, ...b];','copy and merge'],
  ['14','DOM','dom-intro','The DOM is the browser’s live object model of HTML.','The browser turns a document into a tree JavaScript can work with.','# Python has no browser DOM\nconst title = document.querySelector("h1");','document, window, element, node'],
  ['15','DOM Selection','dom-selection','Selectors find the DOM elements your code needs.','Finding a contact in a directory before calling them.','# browser-specific\nconst save = document.querySelector("#save");','querySelector and closest'],
  ['16','DOM Manipulation','dom-manipulation','DOM manipulation changes text, elements, attributes, and classes.','Editing the live page after it has been built.','# browser-specific\nstatus.textContent = "Saved";','textContent vs innerHTML'],
  ['17','Events','events','Events let JavaScript react to actions such as clicks and typing.','A doorbell: the browser signals that something happened.','# browser-specific\nbutton.addEventListener("click", save);','event object and bubbling'],
  ['18','Forms','forms','Forms collect user input and submit it to a server or local code.','A structured request card from a user.','# browser-specific\nconst data = new FormData(form);','validation and submit'],
  ['19','JSON','json','JSON is text used to exchange structured data between systems.','A common shipping label for data between frontend and backend.','import json\ndata = json.loads(text)\nconst data = JSON.parse(text);','JSON string vs JavaScript object'],
  ['20','Modules','modules','Modules split code into reusable files with clear imports and exports.','Organized drawers instead of one huge desk.','from utils import save\nimport { save } from "./utils.js";','named and default exports'],
  ['21','Errors','errors','Errors report that code cannot continue normally.','A dashboard warning that points to something needing attention.','try:\n    run()\nexcept Exception as error:\n    print(error)\ntry {\n  run();\n} catch (error) {\n  console.error(error);\n}','try, catch, throw'],
  ['22','Async JavaScript','async-javascript','Async code lets the browser start slow work and continue handling the page.','Ordering coffee: you do not stand still until it is ready.','# comparable idea: await in Python\n// The event loop returns when waiting work finishes.','call stack and event loop'],
  ['23','Promises','promises','A Promise represents a result that will arrive later or fail.','A delivery tracking card: pending, fulfilled, or rejected.','# similar to a future\nfetch("/api/users").then(showUsers).catch(showError);','then, catch, finally'],
  ['24','Async & Await','async-await','async/await is a readable way to work with Promises.','Write delayed work in the order you want to read it.','async def load():\n    data = await get_users()\nasync function load() {\n  const response = await fetch("/api/users");\n  return response.json();\n}','try/catch'],
  ['25','Fetch & APIs','fetch-apis','fetch sends HTTP requests from JavaScript to an API.','A browser messenger that sends a request and brings back a response.','requests.get(url)\nconst response = await fetch("/api/orders");','GET, POST, headers, body'],
  ['26','Local Storage','local-storage-js','Local Storage saves small string values in one browser.','A small labeled drawer that stays after the tab closes.','# browser-specific\nlocalStorage.setItem("theme", "dark");','JSON and sessionStorage'],
  ['27','Classes','classes','Classes are templates for creating objects with related data and methods.','A blueprint for several similar objects.','class Order:\n    def __init__(self, id):\n        self.id = id\nclass Order {\n  constructor(id) { this.id = id; }\n}','constructor and this'],
  ['28','Map & Set','map-set','Map stores key/value pairs; Set stores unique values.','Map is a labeled lookup table; Set is a guest list with no duplicates.','seen = set()\nconst seen = new Set();','Object vs Map'],
  ['29','Array Methods','array-methods','Array methods transform, select, search, or summarize lists.','A compact toolkit for processing every item in a list.','[x * 2 for x in items]\nconst doubled = items.map(x => x * 2);','map, filter, find, reduce'],
  ['30','Modern JavaScript','modern-javascript','Modern JavaScript combines the syntax most often seen in current web projects.','A concise, practical toolkit for everyday frontend code.','# Compare each feature with its Python equivalent\nconst label = user?.name ?? "Guest";','modern syntax summary']
];
const esc = value => value.replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;');
const sections = (title, syntax, pyjs, focus, slug) => [
  ['what','What is it?', title], ['think','Think of it like...', syntax], ['syntax','Basic Syntax', syntax], ['works','How it works', `JavaScript evaluates this feature when the code reaches it. The result can be stored, returned, displayed, or used to decide what happens next.`], ['simple','Simple Example', pyjs.split('\n').slice(-4).join('\n')], ['web','Real Web Example', `// ${title} in a web page\nconsole.log("${title} is running");`], ['common','Common Syntax', focus], ['mistakes','Common Mistakes', `Do not confuse a similar-looking syntax with the same behavior. Read errors carefully and test with a small value first.`], ['python','Python Comparison', pyjs], ['when','When to use', `Use ${title} when it makes the intent of your code clearer and smaller.`], ['related','Related Concepts', focus], ['reference','Quick Reference', 'Syntax|Meaning\n' + syntax + '|Core pattern\nconsole.log(...)|Inspect a value']
].map(([id,heading,content]) => ({id,heading,content,slug}));
function renderSection(section) {
  if (section.id === 'think') return `<section id="${section.id}" class="card wide topic-section"><h2>${section.heading}</h2><p class="analogy">${esc(section.content)}</p></section>`;
  if (section.id === 'python') { const [python, js] = section.content.split('\nconst ').length > 1 ? [section.content.split('\nconst ')[0], 'const ' + section.content.split('\nconst ').slice(1).join('\nconst ')] : [section.content, section.content]; return `<section id="${section.id}" class="card wide topic-section"><h2>${section.heading}</h2><div class="compare"><div><h3>Python</h3><pre><code>${esc(python)}</code></pre></div><div><h3>JavaScript</h3><pre><code>${esc(js)}</code></pre></div></div></section>`; }
  if (section.id === 'reference') { const rows=section.content.split('\n').slice(1).map(x=>x.split('|')); return `<section id="${section.id}" class="card wide topic-section"><h2>${section.heading}</h2><div class="quick-ref"><table><thead><tr><th>Syntax</th><th>Meaning</th></tr></thead><tbody>${rows.map(r=>`<tr><td><code>${esc(r[0])}</code></td><td>${esc(r[1])}</td></tr>`).join('')}</tbody></table></div></section>`; }
  if (section.id === 'related') return `<section id="${section.id}" class="card wide topic-section"><h2>${section.heading}</h2><div class="related"><a href="javascript-overview.html">JavaScript Overview</a><a href="javascript-summary.html">JavaScript Summary</a><a href="javascript-${section.slug}.html">${esc(section.content)}</a></div></section>`;
  const code = ['syntax','simple','web'].includes(section.id);
  return `<section id="${section.id}" class="card wide topic-section"><h2>${section.heading}</h2>${code ? `<pre><code>${esc(section.content)}</code></pre>` : `<p>${esc(section.content)}</p>`}</section>`;
}
for (let index=0; index<topics.length; index++) {
  const [number,title,slug,description,syntax,pyjs,focus]=topics[index];
  const prev=topics[index-1], next=topics[index+1]; const content=sections(title,syntax,pyjs,focus,slug);
  const toc=content.map(s=>`<a href="#${s.id}">${s.heading}</a>`).join('');
  const pager=`<nav class="prevnext" aria-label="Topic navigation">${prev ? `<a href="javascript-${prev[2]}.html"><small>Previous</small><br>${prev[0]} · ${prev[1]}</a>` : '<a href="javascript-overview.html"><small>Previous</small><br>JavaScript Overview</a>'}${next ? `<a href="javascript-${next[2]}.html"><small>Next</small><br>${next[0]} · ${next[1]}</a>` : '<a href="javascript-summary.html"><small>Next</small><br>JavaScript Summary</a>'}</nav>`;
  const html=`<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${number} · ${title} · JavaScript</title><meta name="description" content="${description}"><link rel="stylesheet" href="topic.css?v=20260913"><link rel="stylesheet" href="site-directory.css"><link rel="stylesheet" href="javascript-topic.css"><script defer src="sidebar.js?v=20260913-javascript"></script></head><body id="top" class="standard-topic has-site-directory js-topic" data-knowledge-type="syntax" data-page-title="${number} · ${title}"><nav><div class="wrap"><a class="brand" href="syntax-overview.html">Rebecca <span>Forward-Deployed AI</span></a></div></nav><aside id="site-directory" aria-label="Language and Syntax site directory"></aside><main class="wrap detail-page"><a class="back" href="javascript-overview.html">← JavaScript Overview</a><header class="hero"><div class="eyebrow">JAVASCRIPT · LANGUAGE &amp; SYNTAX</div><h1>${number} · ${title}</h1><p class="lede">${description}</p></header><div class="js-layout"><div>${content.map(renderSection).join('')}${pager}</div><nav class="on-page" aria-label="On This Page"><b>On This Page</b>${toc}</nav></div></main><a class="back-to-top" href="#top" aria-label="Back to top">↑</a><script>const links=[...document.querySelectorAll('.on-page a')];const sections=[...document.querySelectorAll('.topic-section')];const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting)links.forEach(link=>link.classList.toggle('active',link.hash==='#'+entry.target.id))}),{rootMargin:'-20% 0px -70%'});sections.forEach(section=>observer.observe(section));</script></body></html>`;
  writeFileSync(resolve(root,`javascript-${slug}.html`),html);
}
