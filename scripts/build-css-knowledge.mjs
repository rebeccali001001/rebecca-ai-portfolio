import { writeFileSync } from 'node:fs';

const topics = [
  ['selectors','Selectors','Choose which HTML elements a rule styles.','A selector is an address for elements on a page.','.card p { color: steelblue; }','element, .class, #id, *, [attribute], descendant, > child, + adjacent sibling, ~ general sibling','Use classes for reusable styling; combine selectors when you need a precise target.','Avoid overly deep selectors and relying on #id for ordinary component styling.'],
  ['cascade-specificity','Cascade & Specificity','Decide which competing CSS declaration wins.','Think of several people giving the browser instructions; CSS has tie-break rules.','.notice { color: navy; }\n#sale { color: crimson; }','Cascade, specificity, inheritance, source order, !important','Later rules win when specificity ties. Inline styles beat normal rules; then ID, class/attribute/pseudo-class, and element selectors.','Do not reach for !important first. Inspect the competing rule and its specificity.'],
  ['box-model','Box Model','Describe the rectangular space every element occupies.','An element is a package: content is wrapped by padding, border, then margin.','.card { box-sizing: border-box; width: 320px; padding: 24px; border: 2px solid; margin: 16px; }','content, padding, border, margin, width, height, box-sizing','With border-box, declared width includes padding and border—usually the easier default.','Forgetting that default content-box adds padding and border outside the declared width.'],
  ['units','Units','Measure lengths, sizes, and responsive dimensions.','Units are rulers: some stay fixed, others respond to their container or viewport.','.hero { padding: 4vw; min-height: 100dvh; font-size: 1.25rem; }','px, %, em, rem, vw, vh, dvh, ch','px is absolute-ish; %, em, rem, and viewport units are relative. rem follows the root font size; em follows the current context.','Using vh for mobile full-height sections without considering browser UI; prefer dvh when appropriate.'],
  ['colors-backgrounds','Colors & Backgrounds','Paint text and an element’s background.','They are the palette and wallpaper of a component.','.banner { color: hsl(220 40% 20%); background: linear-gradient(135deg, #dff, rgba(90,120,255,.3)); }','named colors, hex, rgb(), rgba(), hsl(), background-color, image, size, position, repeat','Use background shorthand when it improves readability; gradients are generated images.','Using low-contrast text or confusing color with background-color.'],
  ['typography','Typography','Control how text reads and feels.','Typography is the voice and rhythm of the interface.','body { font-family: Inter, system-ui, sans-serif; font-size: 16px; line-height: 1.6; }','font-family, font-size, font-weight, font-style, line-height, letter-spacing, text-align, text-decoration, text-transform, white-space','A fallback font list lets the browser choose the next available font. Line height is often unitless for reusable text styles.','Using a single unavailable font or very tight line-height for paragraphs.'],
  ['display','Display','Choose how an element participates in layout.','Display is an element’s layout role.','.badge { display: inline-block; }\n.toolbar { display: flex; }','block, inline, inline-block, none, flex, grid, visibility','block starts a new line; inline flows with text; inline-block can have dimensions. display:none removes layout space; visibility:hidden keeps it.','Expecting width and height to work on an ordinary inline element.'],
  ['position','Position','Place an element in normal flow or relative to a containing block.','Position is like choosing whether an object stays in the queue or is pinned to a reference point.','.tooltip { position: absolute; inset: auto 0 100% auto; }','static, relative, absolute, fixed, sticky, top, right, bottom, left, inset','An absolute element is positioned against its nearest positioned ancestor; add position:relative to the card that owns a tooltip.','Using absolute positioning for an entire responsive page layout.'],
  ['flexbox','Flexbox','Lay out items along one main axis with flexible sizing.','Flexbox is a smart row or column that distributes available space.','.row { display: flex; gap: 16px; justify-content: space-between; align-items: center; }\n.item { flex: 1 1 12rem; }','display:flex, flex-direction, justify-content, align-items, align-content, gap, flex-wrap, flex-grow, flex-shrink, flex-basis, flex, order','Main axis follows flex-direction; cross axis is perpendicular. Use gap for space between children.','Mixing up justify-content and align-items, or using order to repair a confusing document order.'],
  ['grid','Grid','Lay out items in rows and columns.','Grid is a two-dimensional table that can adapt without being a literal table.','.cards { display: grid; grid-template-columns: repeat(auto-fit, minmax(14rem, 1fr)); gap: 16px; }','display:grid, grid-template-columns, grid-template-rows, gap, fr, repeat(), minmax(), grid-column, grid-row, auto-fit, auto-fill','fr divides leftover space. auto-fit plus minmax creates responsive cards with fewer media queries.','Forgetting minmax(0, 1fr) when long content needs to shrink inside a column.'],
  ['spacing-sizing','Spacing & Sizing','Set an element’s outer space, inner space, and dimensions.','This is the tape measure for a component.','.content { width: 100%; max-width: 70rem; margin: 0 auto; padding: 24px; aspect-ratio: 16 / 9; }','margin, padding, width, height, min-width, max-width, min-height, max-height, margin:auto, aspect-ratio','width:100% lets a box fill its parent; max-width stops it becoming too wide. Horizontal margin:auto centers a constrained block.','Setting only fixed widths and then wondering why layouts overflow on small screens.'],
  ['borders-shadows','Borders & Shadows','Draw edges, rounding, emphasis, and focus rings.','Borders define the edge; shadows create depth; outlines signal focus.','.card { border: 1px solid #cbd5e1; border-radius: 14px; box-shadow: 0 8px 24px rgb(15 23 42 / .12); }','border, border-width, border-style, border-color, border-radius, box-shadow, outline','Outline does not take up layout space, so it is useful for accessible focus indicators.','Removing outlines without replacing keyboard focus styling.'],
  ['pseudo-classes','Pseudo Classes','Style an element in a state or structural situation.','They are labels the browser temporarily attaches to elements.','button:hover, button:focus-visible { transform: translateY(-1px); }',' :hover, :focus, :active, :visited, :first-child, :last-child, :nth-child(), :not(), :checked, :disabled, :focus-visible','Use :focus-visible to show keyboard focus without adding a ring for every pointer click.','Treating :visited as a general styling hook—browsers restrict it for privacy.'],
  ['pseudo-elements','Pseudo Elements','Style a virtual part of an element.','They are small pieces the browser lets CSS add or target.','.quote::before { content: "“"; }\np::first-letter { font-size: 2em; }','::before, ::after, ::first-letter, ::first-line, ::selection, content','::before and ::after need content to appear, even when it is an empty string.','Putting essential meaning only in generated content; it may not be available to assistive technology.'],
  ['responsive-design','Responsive Design','Make interfaces useful across screen sizes and capabilities.','Responsive design is a plan, not just a media-query list.','.image { max-width: 100%; height: auto; }','mobile-first, fluid layout, max-width, flex, grid, responsive image, breakpoint','Start with a small-screen layout, use fluid sizing, then add a breakpoint only when the design needs one.','Designing a desktop fixed-width layout first and patching it repeatedly for phones.'],
  ['media-queries','Media Queries','Apply CSS only when conditions are true.','A media query is a conditional branch for the environment.','@media (max-width: 768px) { .sidebar { display: none; } }','@media, min-width, max-width, prefers-color-scheme, prefers-reduced-motion','Use min-width in a mobile-first approach. Respect reduced motion by simplifying optional animation.','Choosing device names instead of breakpoints based on where your layout actually breaks.'],
  ['transitions','Transitions','Animate a property smoothly between two states.','A transition is the gentle glide between before and after.','.button { transition: transform .2s ease, background-color .2s ease; }','transition-property, transition-duration, transition-timing-function, transition-delay, transition shorthand','Name the properties you intend to animate. transform and opacity are often efficient choices.','Using transition: all; it can animate unintended and expensive properties.'],
  ['animations','Animations','Run named multi-step motion with keyframes.','An animation is a short timeline the browser plays.','@keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }\n.card { animation: fadeIn .3s ease both; }','@keyframes, animation-name, duration, timing-function, delay, iteration-count, direction, fill-mode','Keyframes define stages; animation shorthand applies the timeline. Provide a reduced-motion alternative for nonessential motion.','Using infinite motion as decoration when it distracts or affects accessibility.'],
  ['variables','CSS Variables','Store reusable custom property values.','Variables are labeled containers for design decisions.',' :root { --spacing-md: 16px; }\n.card { padding: var(--spacing-md, 12px); }',' :root, --custom-property, var(), fallback, scope','Custom properties inherit and can be redefined closer to a component, giving them useful scope.','Assuming a variable is global when it was declared only inside one component.'],
  ['functions','Functions','Calculate or construct CSS values.','Functions are small value factories inside declarations.','h1 { font-size: clamp(2rem, 4vw, 4rem); width: min(100%, 72rem); }','calc(), min(), max(), clamp(), var(), rgb(), hsl(), linear-gradient()','clamp(min, preferred, max) is excellent for fluid typography with safe limits.','Mixing incompatible units in calculations without understanding the resulting value.'],
  ['overflow-z-index','Overflow & Z-index','Control clipped content, scrolling, and painting order.','.panel { overflow: auto; }\n.modal { position: fixed; z-index: 10; }','overflow, overflow-x, overflow-y, visible, hidden, auto, scroll, z-index, stacking context','z-index compares elements inside the same stacking context. A new context can be created by properties such as positioned z-index, transform, or opacity.','Increasing z-index forever when the real issue is a parent stacking context.']
];

const esc = (s) => s.replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;');
const sharedCss = `<style>
.css-knowledge .topic-visual{margin:18px 0;padding:18px;border:1px solid #38516d;border-radius:15px;background:#07111a}.css-knowledge .topic-visual b{display:block;color:var(--green);font-size:12px;letter-spacing:.08em;text-transform:uppercase}.css-knowledge .topic-visual p{margin:8px 0 0}.css-knowledge .key-list{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:10px;margin-top:16px}.css-knowledge .key-list span{padding:11px;border:1px solid #38516d;border-radius:10px;background:#0b1723;color:#dbe7f4;font:600 13px ui-monospace,SFMono-Regular,Consolas,monospace}.css-knowledge pre{overflow:auto;padding:17px;border:1px solid #38516d;border-radius:12px;background:#050b12;color:#dbe7f4;font:14px/1.65 ui-monospace,SFMono-Regular,Consolas,monospace}.css-knowledge .page-nav{display:grid;grid-template-columns:1fr 1fr;gap:14px}.css-knowledge .page-nav a{padding:16px;border:1px solid #38516d;border-radius:14px;background:#07111a}.css-knowledge .page-nav a:last-child{text-align:right}.css-knowledge .page-nav small{color:var(--muted)}.css-knowledge .page-nav strong{display:block;color:#fff;margin-top:4px}.css-knowledge table{width:100%;min-width:680px;border-collapse:collapse}.css-knowledge .table-wrap{overflow:auto;border:1px solid #38516d;border-radius:12px}.css-knowledge th,.css-knowledge td{padding:12px;border-bottom:1px solid #29415a;text-align:left;vertical-align:top}.css-knowledge th{color:var(--green);font-size:11px;letter-spacing:.08em;text-transform:uppercase;background:#102335}@media(max-width:760px){.css-knowledge .key-list,.css-knowledge .page-nav{grid-template-columns:1fr}.css-knowledge .page-nav a:last-child{text-align:left}}</style>`;
const head = (title, description) => `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>${esc(title)} · CSS · Language &amp; Syntax</title><meta name="description" content="${esc(description)}"><link rel="stylesheet" href="topic.css?v=20260910"><link rel="stylesheet" href="site-directory.css"><script defer src="sidebar.js?v=20260913-css-1"></script>${sharedCss}</head>`;
const shell = (title, lede, body) => `${head(title, lede)}<body id="top" class="standard-topic has-site-directory css-knowledge" data-knowledge-type="syntax" data-page-title="${esc(title)}"><nav><div class="wrap"><a class="brand" href="syntax-overview.html">Rebecca <span>Forward-Deployed AI</span></a></div></nav><aside id="site-directory" aria-label="Language and Syntax site directory"></aside><main class="detail-page"><div class="wrap"><a class="back" href="syntax-overview.html">← Language &amp; Syntax</a><div class="eyebrow">WEB · CSS</div><h1>${esc(title)}</h1><p class="lede">${esc(lede)}</p><section class="detail-sections">${body}</section></div></main><a class="back-to-top" href="#top" aria-label="Back to top">↑</a></body></html>`;
const card = (title, content) => `<article class="card detail-section"><h2>${title}</h2>${content}</article>`;
const visualFor = (slug, number, title) => {
  if (slug === 'box-model') return `<div class="topic-visual"><b>Outside → inside</b><p>margin<br>└ border<br>　└ padding<br>　　└ content</p><p>Each layer affects the space an element occupies.</p></div>`;
  if (slug === 'flexbox') return `<div class="topic-visual"><b>Main axis →</b><p>[ item ] &nbsp; [ item ] &nbsp; [ item ]</p><p>Cross axis runs perpendicular. Change flex-direction and the main axis turns vertical.</p></div>`;
  if (slug === 'grid') return `<div class="topic-visual"><b>Two dimensions</b><p>[ 1fr ][ 1fr ][ 1fr ]<br>[ card ][ card ][ card ]</p><p>Grid tracks create columns and rows at the same time.</p></div>`;
  return `<div class="topic-visual"><b>${number} · ${esc(title)}</b><p>Read the code from the target, through the CSS rule, to the visible result in the browser.</p></div>`;
};

const overview = shell('CSS Overview','CSS is the language that turns HTML structure into a readable, responsive visual interface.',
  card('01 · What is CSS?','<p><strong>CSS</strong> means <strong>Cascading Style Sheets</strong>. It controls layout, colors, spacing, typography, responsive behavior, animation, and visual presentation.</p><div class="key-list"><span>Layout</span><span>Colors</span><span>Spacing</span><span>Typography</span><span>Responsive behavior</span><span>Animation</span></div>')+
  card('02 · Structure, presentation, behavior','<div class="topic-visual"><b>Three browser languages</b><p><strong>HTML → structure</strong><br><strong>CSS → presentation</strong><br><strong>JavaScript → behavior</strong></p></div><p>HTML says that a button exists. CSS decides its size, color, and position. JavaScript can make it respond to a click.</p>')+
  card('03 · A CSS rule','<pre><code>selector {\n  property: value;\n}</code></pre><div class="key-list"><span>selector — what to style</span><span>property — setting</span><span>value — chosen setting</span></div><p>The property and value together are a <strong>declaration</strong>.</p>')+
  card('04 · How the browser draws a page','<div class="topic-visual"><b>Rendering path</b><p>HTML ↓ DOM<br>CSS ↓ CSSOM<br>DOM + CSSOM ↓ Render Tree ↓ Layout ↓ Paint</p></div><p>The browser combines structure and styles, calculates where things go, then paints pixels on screen.</p>')+
  card('05 · Start here','<p>Move through the CSS topics in the left directory. Start with selectors, cascade, and the box model; then learn layout with Flexbox and Grid.</p>')+
  card('Continue','<nav class="page-nav"><a href="html.html"><small>Previous system</small><strong>← HTML</strong></a><a href="css-01-selectors.html"><small>Next</small><strong>01 · Selectors →</strong></a></nav>'));
writeFileSync('css.html', overview);

topics.forEach(([slug, title, what, analogy, syntax, keys, works, mistake = 'Avoid using this feature without checking how it affects the surrounding layout and accessibility.'], i) => {
  const number = String(i + 1).padStart(2, '0');
  const previous = i ? `css-${String(i).padStart(2,'0')}-${topics[i-1][0]}.html` : 'css.html';
  const next = i === topics.length - 1 ? 'css-summary.html' : `css-${String(i+2).padStart(2,'0')}-${topics[i+1][0]}.html`;
  const keyItems = keys.split(', ').map(k => `<span>${esc(k)}</span>`).join('');
  const body = card('01 · What is it?', `<p>${esc(what)}</p>`)+
    card('02 · Think of it like...', `<p class="definition">${esc(analogy)}</p>`)+
    card('03 · Basic Syntax', `<pre><code>${esc(syntax)}</code></pre>`)+
    card('04 · Visual Model', visualFor(slug, number, title))+
    card('05 · How it works', `<p>${esc(works)}</p>`)+
    card('06 · Key Properties', `<div class="key-list">${keyItems}</div>`)+
    card('07 · Example', `<pre><code>${esc(syntax)}</code></pre><p>Change one value at a time in DevTools to see what the browser recalculates.</p>`)+
    card('08 · Common Patterns', `<p>Keep a small, reusable rule close to the component it styles. Prefer clear class names and values that support responsive layouts.</p>`)+
    card('09 · Common Mistakes', `<p>${esc(mistake)}</p>`)+
    card('10 · When to use', `<p>Use ${esc(title.toLowerCase())} whenever this is the clearest way to express a visual decision in CSS.</p>`)+
    card('11 · Related Concepts', `<p>Connect this with <a href="css-01-selectors.html">Selectors</a>, <a href="css-02-cascade-specificity.html">Cascade &amp; Specificity</a>, and <a href="css-summary.html">the CSS Summary</a>.</p>`)+
    card('12 · Quick Reference', `<div class="table-wrap"><table><thead><tr><th>Concept</th><th>Meaning</th><th>Example</th></tr></thead><tbody><tr><td>${esc(title)}</td><td>${esc(what)}</td><td><code>${esc(syntax.split('\n')[0])}</code></td></tr></tbody></table></div>`)+
    card('Continue', `<nav class="page-nav"><a href="${previous}"><small>Previous</small><strong>← ${i ? `${String(i).padStart(2,'0')} · ${esc(topics[i-1][1])}` : 'Overview'}</strong></a><a href="${next}"><small>Next</small><strong>${i === topics.length - 1 ? 'Summary' : `${String(i+2).padStart(2,'0')} · ${esc(topics[i+1][1])}`} →</strong></a></nav>`);
  writeFileSync(`css-${number}-${slug}.html`, shell(`${number} · ${title}`, what, body));
});

const summaryRows = topics.map(([slug,title,what,,syntax,keys]) => `<tr><td><a href="css-${String(topics.findIndex(x=>x[0]===slug)+1).padStart(2,'0')}-${slug}.html">${esc(title)}</a></td><td>${esc(keys)}</td><td>${esc(what)}</td><td>${esc(syntax.split('\n')[0])}</td></tr>`).join('');
writeFileSync('css-summary.html', shell('CSS Summary','A compact CSS cheat sheet: use a topic page when you need the why as well as the syntax.', card('CSS Cheat Sheet', `<div class="table-wrap"><table><thead><tr><th>Topic</th><th>Property / Syntax</th><th>Meaning</th><th>Example</th></tr></thead><tbody>${summaryRows}</tbody></table></div>`)+card('Core reminders','<p><strong>Selectors</strong> choose elements. <strong>The cascade</strong> resolves conflicts. <strong>The box model</strong> explains space. <strong>Flexbox and Grid</strong> create layout. <strong>Media queries, fluid sizing, and clamp()</strong> make that layout responsive.</p>')+card('Continue','<nav class="page-nav"><a href="css-21-overflow-z-index.html"><small>Previous</small><strong>← 21 · Overflow &amp; Z-index</strong></a><a href="css.html"><small>Start again</small><strong>CSS Overview →</strong></a></nav>')));
